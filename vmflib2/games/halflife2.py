"""
Helper classes for creating maps in any Source Engine game that uses halflife2.fgd.
This file was auto-generated by import_fgd.py on 2018-06-20 01:47:29.779214.
"""

from vmflib2.vmf import *


class AiAllyManager(Entity):
    """
    Auto-generated from halflife2.fgd, line 2251.
    AI Ally Manager
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_ally_manager", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Maximum number of allies : TODO: Replace this filler.
        self.maxallies = 5
        # Maximum number of medics : TODO: Replace this filler.
        self.maxmedics = 1

        self.auto_properties.extend(["targetname", "maxallies", "maxmedics"])


class AiBattleLine(Entity):
    """
    Auto-generated from halflife2.fgd, line 2430.
    Battle line
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_battle_line", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Actor(s) or squad to affect : TODO: Replace this filler.
        self.actor = ""
        # Active : TODO: Replace this filler.
        self.Active = 0 
        # Strict : Player orders can override, applies to allies only
        self.Strict = 1 

        self.auto_properties.extend(["targetname", "angles", "parentname", "spawnflags", "actor", "Active", "Strict"])


class AiChangehintgroup(Entity):
    """
    Auto-generated from halflife2.fgd, line 2804.
    Change Hint Group
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_changehintgroup", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Search Type : How to search for the entities to change.
        self.SearchType = 0 
        # Name to search for : TODO: Replace this filler.
        self.SearchName = ""
        # New Hint Group : TODO: Replace this filler.
        self.NewHintGroup = ""
        # Search Radius : Radius to search (0 for all of map)
        self.Radius = "0.0" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, does not limit local navigation.
        self.hintlimiting = 0 

        self.auto_properties.extend(["targetname", "SearchType", "SearchName", "NewHintGroup", "Radius", "hintlimiting"])


class AiChangetarget(Entity):
    """
    Auto-generated from halflife2.fgd, line 2788.
    Change Target
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_changetarget", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Target entity : Name of entity whose target will be changed.
        self.target = ""
        # New Target : TODO: Replace this filler.
        self.m_iszNewTarget = ""

        self.auto_properties.extend(["targetname", "target", "m_iszNewTarget"])


class AiCitizenResponseSystem(Entity):
    """
    Auto-generated from halflife2.fgd, line 3042.
    If placed in the level, will manage citizens responses to player's actions.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_citizen_response_system", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""


        self.auto_properties.extend(["targetname"])


class AiGoalActbusy(Entity):
    """
    Auto-generated from halflife2.fgd, line 2715.
    AI Goal Act Busy
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_actbusy", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor(s) to affect : NPC's that should act busy
        self.actor = "" 
        # Start Active : TODO: Replace this filler.
        self.StartActive = 0 
        # Search Type : How to search for the entities using the targetname.
        self.SearchType = 0 
        # Search Range for Busy Hints : TODO: Replace this filler.
        self.busysearchrange = 2048
        # Visible Busy Hints Only : TODO: Replace this filler.
        self.visibleonly = 0 

        # Sight Entity : The Sight Entity (if you provide one) is an entity that will leave the current ActBusy if the Actor playing the ActBusy loses sight of it for the amount of time specified in 'Sight Entity Timeout'. THIS MAY ONLY BE A TARGET NAME. NO CLASSNAMES.
        self.seeentity = "" 
        # Sight Entity Timeout : If you provide a Sight Entity, the Actor will leave the current ActBusy if the Actor has lost sight of Sight Entity for this many seconds.
        self.seeentitytimeout = "1" 
        # Sight Enemy Method : The method to use to determine whether the Sight enemy is visible.
        self.sightmethod = 0 
        # Actbusy Type : TODO: Replace this filler.
        self.type = 0 
        # Combat Safe Zone : Only for combat actbusy. Lets you specify a volume which is the 'safe zone'. The Combat ActBusy will cancel if any enemies are seen in the safe zone.
        self.safezone = "" 
        # Allow actor to teleport? : TODO: Replace this filler.
        self.allowteleport = 0 

        self.auto_properties.extend(["targetname", "actor", "StartActive", "SearchType", "busysearchrange", "visibleonly", "seeentity", "seeentitytimeout", "sightmethod", "type", "safezone", "allowteleport"])


class AiGoalActbusyQueue(Entity):
    """
    Auto-generated from halflife2.fgd, line 2747.
    AI Goal Act Busy Queue
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_actbusy_queue", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor(s) to affect : NPC's that should act busy
        self.actor = "" 
        # Start Active : TODO: Replace this filler.
        self.StartActive = 0 
        # Search Type : How to search for the entities using the targetname.
        self.SearchType = 0 
        # Search Range for Busy Hints : TODO: Replace this filler.
        self.busysearchrange = 2048
        # Visible Busy Hints Only : TODO: Replace this filler.
        self.visibleonly = 0 

        # Exit Node : The name of the node the first NPC in the queue should move to when he leaves the head of the queue.
        self.node_exit = "" 
        # Node 1 : The name of the first actbusy hint node in the queue.
        self.node01 = "" 
        # Node 2 : The name of the second actbusy hint node in the queue.
        self.node02 = "" 
        # Node 3 : The name of the third actbusy hint node in the queue.
        self.node03 = "" 
        # Node 4 : The name of the fourth actbusy hint node in the queue.
        self.node04 = "" 
        # Node 5 : The name of the fifth actbusy hint node in the queue.
        self.node05 = "" 
        # Node 6 : The name of the sixth actbusy hint node in the queue.
        self.node06 = "" 
        # Node 7 : The name of the seventh actbusy hint node in the queue.
        self.node07 = "" 
        # Node 8 : The name of the eighth actbusy hint node in the queue.
        self.node08 = "" 
        # Node 9 : The name of the ninth actbusy hint node in the queue.
        self.node09 = "" 
        # Node 10 : The name of the tenth actbusy hint node in the queue.
        self.node10 = "" 
        # Node 11 : The name of the eleventh actbusy hint node in the queue.
        self.node11 = "" 
        # Node 12 : The name of the twelfth actbusy hint node in the queue.
        self.node12 = "" 
        # Node 13 : The name of the thirteenth actbusy hint node in the queue.
        self.node13 = "" 
        # Node 14 : The name of the fourteenth actbusy hint node in the queue.
        self.node14 = "" 
        # Node 15 : The name of the fifteenth actbusy hint node in the queue.
        self.node15 = "" 
        # Node 16 : The name of the sixteenth actbusy hint node in the queue.
        self.node16 = "" 
        # Node 17 : The name of the seventeenth actbusy hint node in the queue.
        self.node17 = "" 
        # Node 18 : The name of the eighteenth actbusy hint node in the queue.
        self.node18 = "" 
        # Node 19 : The name of the nineteenth actbusy hint node in the queue.
        self.node19 = "" 
        # Node 20 : The name of the twentieth actbusy hint node in the queue.
        self.node20 = "" 
        # Must Reach Front : If true, NPCs much reach the front node in the queue before they're allowed to leave the queue.
        self.mustreachfront = 0 

        self.auto_properties.extend(["targetname", "actor", "StartActive", "SearchType", "busysearchrange", "visibleonly", "node_exit", "node01", "node02", "node03", "node04", "node05", "node06", "node07", "node08", "node09", "node10", "node11", "node12", "node13", "node14", "node15", "node16", "node17", "node18", "node19", "node20", "mustreachfront"])


class AiGoalAssault(Entity):
    """
    Auto-generated from halflife2.fgd, line 2640.
    AI Goal Assault
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_assault", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor(s) to affect : NPC's that should perform this assault
        self.actor = "" 
        # Rally Point Set : Root name of rally points for this assault. Use an asterisk '*' after the root name to match all with the same root.
        self.rallypoint = "" 
        # Search Type : How to search for the entities using the targetname.
        self.SearchType = 0 
        # Start Active : TODO: Replace this filler.
        self.StartActive = 0 
        # Assault Cue : TODO: Replace this filler.
        self.AssaultCue = 1 
        # Rally Point Selection Method : TODO: Replace this filler.
        self.RallySelectMethod = 0 

        self.auto_properties.extend(["targetname", "actor", "rallypoint", "SearchType", "StartActive", "AssaultCue", "RallySelectMethod"])


class AiGoalFollow(Entity):
    """
    Auto-generated from halflife2.fgd, line 2422.
    AI Goal Follow
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_follow", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor(s) to affect : TODO: Replace this filler.
        self.actor = ""
        # Target Entity : The name of the entity to follow. If blank, and the actor likes the player, then defaults to player
        self.goal = ""
        # Search Type : How to search for the entities using the targetname.
        self.SearchType = 0 
        # Start Active : TODO: Replace this filler.
        self.StartActive = 0 
        # Maximum state : TODO: Replace this filler.
        self.MaximumState = 1 
        # Formation : TODO: Replace this filler.
        self.Formation = 0 


        self.auto_properties.extend(["targetname", "actor", "goal", "SearchType", "StartActive", "MaximumState", "Formation"])


class AiGoalInjuredFollow(Entity):
    """
    Auto-generated from halflife2.fgd, line 2426.
    AI Goal Injured Follow
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_injured_follow", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor(s) to affect : TODO: Replace this filler.
        self.actor = ""
        # Target Entity : The name of the entity to follow. If blank, and the actor likes the player, then defaults to player
        self.goal = ""
        # Search Type : How to search for the entities using the targetname.
        self.SearchType = 0 
        # Start Active : TODO: Replace this filler.
        self.StartActive = 0 
        # Maximum state : TODO: Replace this filler.
        self.MaximumState = 1 
        # Formation : TODO: Replace this filler.
        self.Formation = 0 


        self.auto_properties.extend(["targetname", "actor", "goal", "SearchType", "StartActive", "MaximumState", "Formation"])


class AiGoalLead(Entity):
    """
    Auto-generated from halflife2.fgd, line 2356.
    AI Goal Lead
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_lead", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor(s) to affect : TODO: Replace this filler.
        self.actor = ""
        # Target Entity : TODO: Replace this filler.
        self.goal = ""
        # Point to wait at if the target's not visible : TODO: Replace this filler.
        self.WaitPointName = ""
        # Wait until player gets this close : TODO: Replace this filler.
        self.WaitDistance = ""
        # Lead Distance : The player is considered to be lagging if he's beyond this distance.  The Actor will consider retrieving when the player is 4x 'Lead Distance' away.
        self.LeadDistance = "64" 
        # Retrieve Distance : The distance from the player that the NPC should return to when retrieving a lagging player. Must be between ('Lead Distance' + 24) and ('Lead Distance' * 4) to avoid the leader ping-ponging.
        self.RetrieveDistance = "96" 
        # Success Distance : The distance from the player (to the NPC) that the player must be within for the Lead to succeed, once the NPC has reached the goal. If set to 0, it'll use the lead distance instead (for legacy support).
        self.SuccessDistance = "0" 
        # Run instead of Walk : TODO: Replace this filler.
        self.Run = "0" 
        # Retrieve player? : TODO: Replace this filler.
        self.Retrieve = 1 
        # Before Coming Back, Wait for speech? : TODO: Replace this filler.
        self.ComingBackWaitForSpeak = 1 
        # On Retrieve, Wait for speech? : TODO: Replace this filler.
        self.RetrieveWaitForSpeak = 1 
        # Speak start greeting? : TODO: Replace this filler.
        self.DontSpeakStart = 0 
        # Lead during combat? : TODO: Replace this filler.
        self.LeadDuringCombat = 0 
        # Gag Leader? : TODO: Replace this filler.
        self.GagLeader = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.AttractPlayerConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.WaitOverConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.ArrivalConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.PostArrivalConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.SuccessConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.FailureConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.ComingBackConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.RetrieveConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # Search Type : How to search for the entities using the targetname.
        self.SearchType = 0 

        self.auto_properties.extend(["targetname", "actor", "goal", "WaitPointName", "WaitDistance", "LeadDistance", "RetrieveDistance", "SuccessDistance", "Run", "Retrieve", "ComingBackWaitForSpeak", "RetrieveWaitForSpeak", "DontSpeakStart", "LeadDuringCombat", "GagLeader", "AttractPlayerConceptModifier", "WaitOverConceptModifier", "ArrivalConceptModifier", "PostArrivalConceptModifier", "SuccessConceptModifier", "FailureConceptModifier", "ComingBackConceptModifier", "RetrieveConceptModifier", "spawnflags", "SearchType"])


class AiGoalLeadWeapon(Entity):
    """
    Auto-generated from halflife2.fgd, line 2365.
    AI Goal Lead (Weapon). A version of the ai_goal_lead entity that requires the player to have the specified weapon before the Actor(s) will lead the player to their target.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_lead_weapon", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor(s) to affect : TODO: Replace this filler.
        self.actor = ""
        # Target Entity : TODO: Replace this filler.
        self.goal = ""
        # Point to wait at if the target's not visible : TODO: Replace this filler.
        self.WaitPointName = ""
        # Wait until player gets this close : TODO: Replace this filler.
        self.WaitDistance = ""
        # Lead Distance : The player is considered to be lagging if he's beyond this distance.  The Actor will consider retrieving when the player is 4x 'Lead Distance' away.
        self.LeadDistance = "64" 
        # Retrieve Distance : The distance from the player that the NPC should return to when retrieving a lagging player. Must be between ('Lead Distance' + 24) and ('Lead Distance' * 4) to avoid the leader ping-ponging.
        self.RetrieveDistance = "96" 
        # Success Distance : The distance from the player (to the NPC) that the player must be within for the Lead to succeed, once the NPC has reached the goal. If set to 0, it'll use the lead distance instead (for legacy support).
        self.SuccessDistance = "0" 
        # Run instead of Walk : TODO: Replace this filler.
        self.Run = "0" 
        # Retrieve player? : TODO: Replace this filler.
        self.Retrieve = 1 
        # Before Coming Back, Wait for speech? : TODO: Replace this filler.
        self.ComingBackWaitForSpeak = 1 
        # On Retrieve, Wait for speech? : TODO: Replace this filler.
        self.RetrieveWaitForSpeak = 1 
        # Speak start greeting? : TODO: Replace this filler.
        self.DontSpeakStart = 0 
        # Lead during combat? : TODO: Replace this filler.
        self.LeadDuringCombat = 0 
        # Gag Leader? : TODO: Replace this filler.
        self.GagLeader = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.AttractPlayerConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.WaitOverConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.ArrivalConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.PostArrivalConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.SuccessConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.FailureConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.ComingBackConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.RetrieveConceptModifier = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # Weapon : TODO: Replace this filler.
        self.WeaponName = "weapon_bugbait" 
        # Missing weapon concept modifier : TODO: Replace this filler.
        self.MissingWeaponConceptModifier = ""
        # Search Type : How to search for the entities using the targetname.
        self.SearchType = 0 

        self.auto_properties.extend(["targetname", "actor", "goal", "WaitPointName", "WaitDistance", "LeadDistance", "RetrieveDistance", "SuccessDistance", "Run", "Retrieve", "ComingBackWaitForSpeak", "RetrieveWaitForSpeak", "DontSpeakStart", "LeadDuringCombat", "GagLeader", "AttractPlayerConceptModifier", "WaitOverConceptModifier", "ArrivalConceptModifier", "PostArrivalConceptModifier", "SuccessConceptModifier", "FailureConceptModifier", "ComingBackConceptModifier", "RetrieveConceptModifier", "spawnflags", "WeaponName", "MissingWeaponConceptModifier", "SearchType"])


class AiGoalOperator(Entity):
    """
    Auto-generated from halflife2.fgd, line 1857.
    Indicates items in the world that some NPCs may operate upon
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_operator", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Actor to affect : NPC that should perform this operation
        self.actor = "" 
        # Position entity : Name of the entity that the NPC should move to in order to perform the operation.
        self.target = "" 
        # Context target : (Optional) Name of an entity that the operator will use within context.
        self.contexttarget = "" 
        # Initial State : TODO: Replace this filler.
        self.state = 0 
        # How should NPC approach? : TODO: Replace this filler.
        self.moveto = 1 

        self.auto_properties.extend(["targetname", "StartDisabled", "actor", "target", "contexttarget", "state", "moveto"])


class AiGoalPolice(Entity):
    """
    Auto-generated from halflife2.fgd, line 2528.
    AI Goal Police
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_police", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Radius : Radius to police
        self.policeradius = 512 
        # Target : Target to police
        self.policetarget = "" 

        self.auto_properties.extend(["targetname", "parentname", "angles", "spawnflags", "policeradius", "policetarget"])


class AiGoalStandoff(Entity):
    """
    Auto-generated from halflife2.fgd, line 2457.
    AI Goal Standoff
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_goal_standoff", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor(s) to affect : TODO: Replace this filler.
        self.actor = ""
        # Search Type : How to search for the entities using the targetname.
        self.SearchType = 0 
        # Start Active : TODO: Replace this filler.
        self.StartActive = 0 
        # Reaction to tactical change : What to do if leader moves, threat is neutralized, hint group changes, etc
        self.HintGroupChangeReaction = 1 
        # Aggressiveness : TODO: Replace this filler.
        self.Aggressiveness = 2 
        # Player battleline : Player defines a battle line, applies to allies only
        self.PlayerBattleline = 1 
        # Stay at cover location : When have suitable cover, don't change it (disables advancing to battle line)
        self.StayAtCover = 0 
        # Abandon if enemies hide : If no enemy detected recently, stop the standoff
        self.AbandonIfEnemyHides = 0 

        self.auto_properties.extend(["targetname", "actor", "SearchType", "StartActive", "HintGroupChangeReaction", "Aggressiveness", "PlayerBattleline", "StayAtCover", "AbandonIfEnemyHides"])


class AiNpcEventresponsesystem(Entity):
    """
    Auto-generated from halflife2.fgd, line 2798.
    An entity that allows you to generate events for nearby friendly NPCs to respond to.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_npc_eventresponsesystem", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""


        self.auto_properties.extend(["targetname"])


class AiRelationship(Entity):
    """
    Auto-generated from halflife2.fgd, line 2208.
    AI Relationship - Sets relationships between groups of NPCs in the AI.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_relationship", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Subject(s) : This is the NPC(s) whose disposition will change. May be a targetname or a classname.
        self.subject = "" 
        # Target(s) : This is the NPC(s) about whom the Subject(s) will change their disposition. May be a tarGetname or a classname.
        self.target = "" 
        # Disposition : Choose the way the Subject(s) should feel about the Target(s)
        self.disposition = 3 
        # Radius for subject : TODO: Replace this filler.
        self.radius = 0
        # Disposition Priority : How much the Subject(s) should Like/Hate/Fear the Target(s). Higher priority = stronger feeling.
        self.rank = 0 
        # Start Active : TODO: Replace this filler.
        self.StartActive = 0 
        # Reciprocal : Set this to YES to have the new relationship mirrored by Target
        self.Reciprocal = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "subject", "target", "disposition", "radius", "rank", "StartActive", "Reciprocal", "spawnflags"])


class AiScriptConditions(Entity):
    """
    Auto-generated from halflife2.fgd, line 2827.
    AI Script Conditions
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_script_conditions", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Actor : NPC Target
        self.Actor = ""
        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 1 
        # Minimum state : TODO: Replace this filler.
        self.MinimumState = 1 
        # Maximum state : TODO: Replace this filler.
        self.MaximumState = 3 
        # Actor is running a script? : TODO: Replace this filler.
        self.ScriptStatus = 2 
        # Required Time : Duration of time that all the conditions must be true
        self.RequiredTime = 0 
        # Minimum time out : Minimum time before OnConditionsTimeout is fired. 0 = never expire.
        self.MinTimeout = 0 
        # Maximum time out : Maximum time before OnConditionsTimeout is fired. 0 = ignore (If you don't specify a Maximum timeout, conditions will time out at exactly Minimum Time Out. If you DO specify a Maximum time out, timeout will occur randomly between Minimum and Maximum time out values.)
        self.MaxTimeout = 0 
        # Actor Sees Player : TODO: Replace this filler.
        self.ActorSeePlayer = 2 
        # Player distance : The distance the player must/must not be to the actor. Negative values for NOT, 0 for ignore.
        self.PlayerActorProximity = 0 
        # Player FOV for Actor  : Specify angle of view cone in degrees. Negative value = NOT
        self.PlayerActorFOV = 360 
        # Play FOV to Actor is a true view cone : Player's view cone is evaluated as a true cone, not pie slice 
        self.PlayerActorFOVTrueCone = 0 
        # Player has LOS to Actor : Checks that the player has clear Line of Sight to the Actor
        self.PlayerActorLOS = 2 
        # Target (Optional) : Optional entity to include in conditions
        self.target = ""
        # Actor Sees Target : TODO: Replace this filler.
        self.ActorSeeTarget = 2 
        # Target distance : The distance the actor must/must not be to the Target. Negative values for NOT, 0 for ignore.
        self.ActorTargetProximity = 0 
        # Player distance from Target : The distance the player must/must not be to the Target. Negative values for NOT, 0 for ignore.
        self.PlayerTargetProximity = 0 
        # Player FOV for Target : Specify angle of view cone in degrees. Negative value = NOT
        self.PlayerTargetFOV = 360 
        # Play FOV to Target is a true view cone : Player's view cone is evaluated as a true cone, not pie slice 
        self.PlayerTargetFOVTrueCone = 0 
        # Player has LOS to Target : Checks that the player has clear Line of Sight to the Target
        self.PlayerTargetLOS = 2 
        # Player blocking Actor : Checks that the player is blocking the Actor's path
        self.PlayerBlockingActor = 2 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.ActorInPVS = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.ActorInVehicle = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.PlayerInVehicle = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "Actor", "StartDisabled", "MinimumState", "MaximumState", "ScriptStatus", "RequiredTime", "MinTimeout", "MaxTimeout", "ActorSeePlayer", "PlayerActorProximity", "PlayerActorFOV", "PlayerActorFOVTrueCone", "PlayerActorLOS", "target", "ActorSeeTarget", "ActorTargetProximity", "PlayerTargetProximity", "PlayerTargetFOV", "PlayerTargetFOVTrueCone", "PlayerTargetLOS", "PlayerBlockingActor", "ActorInPVS", "ActorInVehicle", "PlayerInVehicle", "spawnflags"])


class AiSound(Entity):
    """
    Auto-generated from halflife2.fgd, line 3842.
    This entity makes sounds or smells that can be sensed by NPCs, butnot by the player. This can be used to cause reactions in nearby NPCs.\n\nSound Types\n  Combat: Will cause most NPCs to become alert\n  World: Will cause most NPCs to become alert\n  Danger: Will cause most NPCs to move away from the position of the sound\n  Bullet Impact: \n  Carcass: \n  Meat: \n  Garbage: \n  Thumper: causes antlions to run away briefly\n  Readiness: (Low, Medium, High) Causes player companions that can hear this sound to change readiness\n
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "ai_sound", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Volume : How far away this sound can be heard. This is a radius.
        self.volume = 120 
        # Duration : How long the sound persists each time you insert it.
        self.duration = "0.5" 
        # Sound Type : The type of sound or smell will determine the reaction of NPCs that sense it.
        self.soundtype = 0 
        # Additional sound context (optional) : Optional settings specifying such things as who can or cannot hear the sound.
        self.soundcontext = 0 
        # Location Proxy : The name of an entity to use as a proxy to determine the location at which to make the sound. If you specify an entity here, the sound will be made at that entity's location (!player included)
        self.locationproxy = "" 

        self.auto_properties.extend(["targetname", "parentname", "volume", "duration", "soundtype", "soundcontext", "locationproxy"])


class AiscriptedSchedule(Entity):
    """
    Auto-generated from halflife2.fgd, line 2989.
    Issues a command to an NPC without taking the NPC out of its AI. This does not seize control of the NPC as a scripted_sequence does
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "aiscripted_schedule", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Target NPC : The name or classname of an NPC to use.
        self.m_iszEntity = ""
        # Search Radius (0=everywhere) : Radius to search within for an NPC to use. 0 searches everywhere.
        self.m_flRadius = 0 
        # All in radius : Whether to grab all matching NPCs in the specified radius, instead of just one
        self.graball = 0
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # AI state to set : TODO: Replace this filler.
        self.forcestate = 0 
        # Schedule to run : TODO: Replace this filler.
        self.schedule = 1 
        # Interruptability : TODO: Replace this filler.
        self.interruptability = 0 
        # Goal entity : Provides the name of a schedule-specific goal entity (see 'Schedule to run')
        self.goalent = ""

        self.auto_properties.extend(["targetname", "m_iszEntity", "m_flRadius", "graball", "spawnflags", "forcestate", "schedule", "interruptability", "goalent"])


class AssaultAssaultpoint(Entity):
    """
    Auto-generated from halflife2.fgd, line 2574.
    (Assault) assault point
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "assault_assaultpoint", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Assault Hint Group : NPC's movements are constrained to this hint group once assault has begun
        self.assaultgroup = "" 
        # Next assault point (optional) : TODO: Replace this filler.
        self.nextassaultpoint = ""
        # Assault time out : This point is cleared when no enemies are seen for this long (seconds)
        self.assaulttimeout = "3.0" 
        # Clear on contact with enemies : If you come in contact with enemies while approaching the assault point, clear our assault point
        self.clearoncontact = 0 
        # Allow diversion : If you come in contact with enemies while approaching the assault point, divert to kill them. Resume the assault once contact is lost.
        self.allowdiversion = 0 
        # Diversion Proximity : If Allow Diversion is set, NPC will only divert from assault to attack an enemy that is within this distance of the assault point. 0 = No limit.
        self.allowdiversionradius = 0 
        # Never Timeout : If set, the assault never ends for NPCs assaulting this point. Useful for forcing NPCs back to a point.
        self.nevertimeout = 0 
        # Strict? : TODO: Replace this filler.
        self.strict = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Force Crouch : NPCs using this assault point are forced into crouching while holding it.
        self.forcecrouch = 0 
        # Urgent : If true, NPCs will consider movement to this assault point as Urgent Navigation.
        self.urgent = 0 
        # Attack Tolerance : How far this NPC may move from the assault point to try to attack an enemy.
        self.assaulttolerance = 36 

        self.auto_properties.extend(["targetname", "parentname", "angles", "assaultgroup", "nextassaultpoint", "assaulttimeout", "clearoncontact", "allowdiversion", "allowdiversionradius", "nevertimeout", "strict", "spawnflags", "forcecrouch", "urgent", "assaulttolerance"])


class AssaultRallypoint(Entity):
    """
    Auto-generated from halflife2.fgd, line 2551.
    (Assault) rally point
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "assault_rallypoint", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Assault Point : Location to move to as assault begins
        self.assaultpoint = "" 
        # Assault Delay : How long to wait after cue is given before assault begins.
        self.assaultdelay = 0 
        # Rally Sequence : Override the NPC's wait activity by entering a sequence name.
        self.rallysequence = "" 
        # Priority : Higher priority rally points get selected first.
        self.priority = 1 
        # Force Crouch : NPCs using this assault point are forced into crouching while holding it.
        self.forcecrouch = 0 
        # Urgent : If true, NPCs will consider movement to this rally point as Urgent Navigation.
        self.urgent = 0 

        self.auto_properties.extend(["targetname", "parentname", "angles", "assaultpoint", "assaultdelay", "rallysequence", "priority", "forcecrouch", "urgent"])


class CombineMine(Entity):
    """
    Auto-generated from halflife2.fgd, line 3901.
    Combine Land Mine
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "combine_mine", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Bounce : Whether the mine should bounce up in the air before exploding.
        self.bounce = 1 
        # Lock Silently : If yes, mine plays no sound when clamps close
        self.LockSilently = 1 
        # Start Disarmed : If yes, mine begins dormant.
        self.StartDisarmed = 0 
        # Citizen modified : 'Normal' is default Combine behavior. 'Cavern' detonates earlier in its jump, and has a different default skin.
        self.Modification = 0 

        self.auto_properties.extend(["targetname", "parentname", "bounce", "LockSilently", "StartDisarmed", "Modification"])


class CyclerActor(Entity):
    """
    Auto-generated from halflife2.fgd, line 1972.
    Actor Cycler
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "cycler_actor", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Model : TODO: Replace this filler.
        self.model = ""
        # Sentence Group : TODO: Replace this filler.
        self.Sentence = ""

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "disableshadows", "model", "Sentence"])


class EnvAlyxemp(Entity):
    """
    Auto-generated from halflife2.fgd, line 4493.
    Special effect for the Alyx's EMP device.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_alyxemp", vmf_map)

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # EMP Type : TODO: Replace this filler.
        self.Type = 0 
        # Target Entity : Entity to use as a target endpoint.
        self.EndTargetName = "" 

        self.auto_properties.extend(["angles", "targetname", "parentname", "Type", "EndTargetName"])


class EnvAr2explosion(Entity):
    """
    Auto-generated from halflife2.fgd, line 3934.
    AR2 explosion visual effect. Big, volume-filling brown cloud. Does not cause damage or emit sound.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_ar2explosion", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Particle Material : The material to use for each particle in the explosion.
        self.material = "particle/particle_noisesphere" 

        self.auto_properties.extend(["targetname", "parentname", "material"])


class EnvCitadelEnergyCore(Entity):
    """
    Auto-generated from halflife2.fgd, line 4477.
    Special effect for the energy cores in citadel.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_citadel_energy_core", vmf_map)

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Scale : Scale of the effect.  1 is the default size, 2 is twice that, etc.
        self.scale = 1 

        self.auto_properties.extend(["angles", "targetname", "parentname", "spawnflags", "scale"])


class EnvEntityDissolver(Entity):
    """
    Auto-generated from halflife2.fgd, line 4573.
    Entity Dissolver
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_entity_dissolver", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Target to Dissolve : Targetname of the entity you want to dissolve.
        self.target = "" 
        # Magnitude : How strongly to push away from the center.
        self.magnitude = 250 
        # Dissolve Type : TODO: Replace this filler.
        self.dissolvetype = "Energy" 

        self.auto_properties.extend(["targetname", "target", "magnitude", "dissolvetype"])


class EnvFlare(Entity):
    """
    Auto-generated from halflife2.fgd, line 3948.
    Flare
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_flare", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Scale : TODO: Replace this filler.
        self.scale = 1
        # Duration : TODO: Replace this filler.
        self.duration = 30
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "parentname", "angles", "scale", "duration", "spawnflags"])


class EnvGlobal(Entity):
    """
    Auto-generated from halflife2.fgd, line 3126.
    An entity to control a game-specific global states.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_global", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Initial State : TODO: Replace this filler.
        self.initialstate = 0 
        # Counter : An integer counter value associated with this global.
        self.counter = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # Global State to Set : TODO: Replace this filler.
        self.globalstate = ""

        self.auto_properties.extend(["targetname", "initialstate", "counter", "spawnflags", "globalstate"])


class EnvGunfire(Entity):
    """
    Auto-generated from halflife2.fgd, line 1116.
    Gunfire Effect
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_gunfire", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Target : Shoot at this target. REMEMBER - this is an effect only! It does not do damage!
        self.target = "" 
        # Min Burst Size : Minimum number of rounds in a burst.
        self.minburstsize = 2 
        # Max Burst Size : Maximum number of rounds in a burst.
        self.maxburstsize = 7 
        # Min Delay Between Bursts : Minimum delay between bursts. (seconds)
        self.minburstdelay = 2 
        # Max Delay Between Bursts : Maximum delay between bursts. (seconds)
        self.maxburstdelay = 5 
        # Rate of fire : Expressed as rounds per second
        self.rateoffire = 10 
        # Bullet spread : The 'cone of inaccuracy' of the shots fired by this entity.
        self.spread = 5 
        # Bullet distribution should be... : How to distribute bullets within the spread. Even distribution is a true scatter throughout the spread. Biased towards the outside makes the shots 'miss' the target by tending towards the outside of the spread.
        self.bias = 1 
        # Collision detection : Whether/how to handle bullet collision detection. NOTE: If you select NONE, this entity will be very cheap to use, but all bullets will stop short at their target's position in space and there will be no impact effects. Normal collision detection does the same things NPCs do when they fire their guns (except harm anything).
        self.collisions = 0 
        # Shoot Sound : Gunfire sound to make
        self.shootsound = "Weapon_AR2.NPC_Single" 
        # Tracer : Type of tracer to display
        self.tracertype = "AR2TRACER" 

        self.auto_properties.extend(["targetname", "parentname", "targetname", "StartDisabled", "target", "minburstsize", "maxburstsize", "minburstdelay", "maxburstdelay", "rateoffire", "spread", "bias", "collisions", "shootsound", "tracertype"])


class EnvHeadcrabcanister(Entity):
    """
    Auto-generated from halflife2.fgd, line 1161.
    Headcrab canister
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_headcrabcanister", vmf_map)

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Which headcrab to spawn? : TODO: Replace this filler.
        self.HeadcrabType = 0 
        # Headcrab count : Number of headcrabs to spawn on impact
        self.HeadcrabCount = 6 
        # Flight Speed : Speed to fly through the air
        self.FlightSpeed = 3000  
        # Flight Time : Time to fly through the air in seconds
        self.FlightTime = 5	    
        # Starting Height : Relative height from the landing position at which the canister should be launched. Positive values mean launch it above the impact point, negative values mean launch it below.
        self.StartingHeight = 0 
        # Min Refire Time : Min number of seconds before the cannister is refired. This will only work for cannisters placed in the skybox.
        self.MinSkyboxRefireTime = 0 
        # Max Refire Time : Max number of seconds before the cannister is refired. This will only work for cannisters placed in the skybox.
        self.MaxSkyboxRefireTime = 0 
        # Cannister count : Number of cannisters to fire in the skybox (0 means fire continuously, forever).
        self.SkyboxCannisterCount = 1 
        # Impact damage : Max damage the canister applies on impact
        self.Damage = 150 
        # Impact damage radius : Max radius of the impact damage for the canister
        self.DamageRadius = 750 
        # Smoke Duration : Duration that the canister smokes.  -1 means always smoke.
        self.SmokeLifetime = 30 
        # Launch Position Name : If the canister should launch to it's origin from another point within the world, this should specify an info_target at the launch origin.
        self.LaunchPositionName = "" 

        self.auto_properties.extend(["parentname", "angles", "targetname", "spawnflags", "HeadcrabType", "HeadcrabCount", "FlightSpeed", "FlightTime", "StartingHeight", "MinSkyboxRefireTime", "MaxSkyboxRefireTime", "SkyboxCannisterCount", "Damage", "DamageRadius", "SmokeLifetime", "LaunchPositionName"])


class EnvMuzzleflash(Entity):
    """
    Auto-generated from halflife2.fgd, line 3967.
    Muzzle Flash
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_muzzleflash", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Parent Attachment : The name of an attachment on the parent to hierarchically attach to.
        self.parentattachment = "" 
        # Scale : Size of the muzzle flash.
        self.scale = "1" 

        self.auto_properties.extend(["targetname", "parentname", "angles", "parentattachment", "scale"])


class EnvRotorwash(Entity):
    """
    Auto-generated from halflife2.fgd, line 3891.
    Rotorwash Effect
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_rotorwash", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "parentname", "spawnflags"])


class EnvSpeaker(Entity):
    """
    Auto-generated from halflife2.fgd, line 4432.
    Announcement Speaker
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_speaker", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Min Delay Between Announcements : TODO: Replace this filler.
        self.delaymin = "15"
        # Max Delay Between Announcements : TODO: Replace this filler.
        self.delaymax = "135"
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Context rule script : Script file containing rules for playing appropriate sounds.
        self.rulescript = "" 
        # Concept name : High level concept name used as primary search key.
        self.concept = "" 


        self.auto_properties.extend(["targetname", "ResponseContext", "delaymin", "delaymax", "spawnflags", "rulescript", "concept"])


class EnvSporeexplosion(Entity):
    """
    Auto-generated from halflife2.fgd, line 1111.
    Bugbait Spore Effect
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_sporeexplosion", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Spawn Rate (as percentage) : How dense the spore effect is
        self.spawnrate = "25" 

        self.auto_properties.extend(["targetname", "parentname", "StartDisabled", "spawnrate"])


class EnvStarfield(Entity):
    """
    Auto-generated from halflife2.fgd, line 3941.
    Starfield effect
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_starfield", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""


        self.auto_properties.extend(["targetname"])


class EnvTerrainmorph(Entity):
    """
    Auto-generated from halflife2.fgd, line 3976.
    Morphs terrain by pulling vertices along a normal.\n\nPlace this entity the desired distance from a terrain surface and set ANGLES to the normal along which you want to pull the surface's vertices. If you set the INSTANT spawnflag, the morph will take place instantly instead of over time.\n
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "env_terrainmorph", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Start Radius : Radius of the effect when morphing begins. Only this value is used
        self.startradius = 500 
        # Goal Radius : Radius of the effect at the end of morphing. The radius of this effect will change from 
        self.goalradius = 100 
        # Duration : The morph will take place over this period of time (seconds). Ignored if flagged to occur instantly
        self.duration = 3 
        # Displacement Fraction : If set to 1, the terrain surface will be pulled exactly to this entity's position.
        self.fraction = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "parentname", "startradius", "goalradius", "duration", "fraction", "spawnflags"])


class FilterCombineballType(Entity):
    """
    Auto-generated from halflife2.fgd, line 4562.
    A filter that filters by combine ball type.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "filter_combineball_type", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Filter mode : If set to Allow, only entities who match the criteria will pass the filter. 
        self.Negated = "Allow entities that match criteria" 

        # Ball Type : TODO: Replace this filler.
        self.balltype = 1 

        self.auto_properties.extend(["targetname", "Negated", "balltype"])


class FuncBulletshield(Entity):
    """
    Auto-generated from halflife2.fgd, line 4217.
    A shield that stops only bullets.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_bulletshield", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Input Filter : Used to specify which inputs this entity will accept.
        self.InputFilter = 0 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Minimum Light Level : The minimum level of ambient light that hits this brush.
        self._minlight = ""
        # Solidity : Used to control the solidity/collision of these brushes.
        self.Solidity = 0 
        # NPC class excluded from collisions : If an NPC classname is specified here, NPCs of that type won't collide with these brushes. In Episodic, you may also specify an individual entity's name.
        self.excludednpc = "" 
        # Invert NPC class exclusion : If set, then the excluded NPC class will consider this brush solid, and all other NPC classes will consider it non-solid.
        self.invert_exclusion = 0 
        # Solid BSP : Set this if this brush is in heirarchy with a moving object of some kind, and the player can stand on this brush.
        self.solidbsp = 0 
        # Shadows : Set this if this brush casts lightmap shadows.
        self.vrad_brush_cast_shadows = 0 


        self.auto_properties.extend(["targetname", "parentname", "origin", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "InputFilter", "StartDisabled", "disableshadows", "spawnflags", "_minlight", "Solidity", "excludednpc", "invert_exclusion", "solidbsp", "vrad_brush_cast_shadows"])


class FuncCombineBallSpawner(Entity):
    """
    Auto-generated from halflife2.fgd, line 171.
    Spawns Combine balls.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_combine_ball_spawner", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Ball count : This is how many balls will be bouncing around inside the spawner
        self.ballcount = 3 
        # Min ball speed : The minimum speed of balls that fly in the spawner
        self.minspeed = "300.0" 
        # Max ball speed : The maximum speed of balls that fly in the spawner
        self.maxspeed = "600.0" 
        # Ball radius : The radius of the energy balls
        self.ballradius = "20.0" 
        # Ball Type : TODO: Replace this filler.
        self.balltype = "Combine Energy Ball 1" 
        # Ball Respawn Time : The energy balls respawn time
        self.ballrespawntime = "4.0f" 


        self.auto_properties.extend(["targetname", "origin", "angles", "globalname", "spawnflags", "ballcount", "minspeed", "maxspeed", "ballradius", "balltype", "ballrespawntime"])


class FuncHealthcharger(Entity):
    """
    Auto-generated from halflife2.fgd, line 3055.
    Wall health recharger
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_healthcharger", vmf_map)

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""

        self.auto_properties.extend(["StartDisabled", "parentname", "origin", "globalname", "_minlight"])


class FuncLookdoor(Entity):
    """
    Auto-generated from halflife2.fgd, line 3094.
    A door that moves either when looked by a targeted object or when a target object comes near the door.  Behavior can be either based on viewing direction or proximity alone, or on a combination of both.  If inverted the doors behavior will be the opposite.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_lookdoor", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Move Direction (Pitch Yaw Roll) : The direction the brushes will move, when told to.
        self.movedir = "0 0 0" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Position : Position of brush when spawned. The range is a value between 0.0 and 1.0, where 0 is the starting position and 1 is the starting position + (move direction * move distance).
        self.startposition = 0 
        # Speed : The speed that the brush moves, in inches per second.
        self.speed = 100 
        # Move Distance : The distance from the starting point that the brush should move, in inches.
        self.movedistance = 100 
        # Block Damage : The amount of damage to do to any entity that blocks the brushes, per frame.
        self.blockdamage = 0 
        # Sound played when the brush starts moving. : TODO: Replace this filler.
        self.startsound = ""
        # Sound played when the brush stops moving. : TODO: Replace this filler.
        self.stopsound = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Proximity Distance : If non-zero, proximity range over which door will move
        self.ProximityDistance = "0.0"  
        # Proximity Offset : Offset from the target object
        self.ProximityOffset = "0.0" 
        # FieldOfView : If non-zero, field of view over which door will move
        self.FieldOfView = "0.0"  

        self.auto_properties.extend(["targetname", "parentname", "origin", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "movedir", "spawnflags", "startposition", "speed", "movedistance", "blockdamage", "startsound", "stopsound", "spawnflags", "ProximityDistance", "ProximityOffset", "FieldOfView"])


class FuncMonitor(Entity):
    """
    Auto-generated from halflife2.fgd, line 4205.
    A monitor that renders the view from a given point_camera entity.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_monitor", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Input Filter : Used to specify which inputs this entity will accept.
        self.InputFilter = 0 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Minimum Light Level : The minimum level of ambient light that hits this brush.
        self._minlight = ""
        # Solidity : Used to control the solidity/collision of these brushes.
        self.Solidity = 0 
        # NPC class excluded from collisions : If an NPC classname is specified here, NPCs of that type won't collide with these brushes. In Episodic, you may also specify an individual entity's name.
        self.excludednpc = "" 
        # Invert NPC class exclusion : If set, then the excluded NPC class will consider this brush solid, and all other NPC classes will consider it non-solid.
        self.invert_exclusion = 0 
        # Solid BSP : Set this if this brush is in heirarchy with a moving object of some kind, and the player can stand on this brush.
        self.solidbsp = 0 
        # Shadows : Set this if this brush casts lightmap shadows.
        self.vrad_brush_cast_shadows = 0 

        # Camera name : TODO: Replace this filler.
        self.target = ""

        self.auto_properties.extend(["targetname", "parentname", "origin", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "InputFilter", "StartDisabled", "disableshadows", "spawnflags", "_minlight", "Solidity", "excludednpc", "invert_exclusion", "solidbsp", "vrad_brush_cast_shadows", "target"])


class FuncRecharge(Entity):
    """
    Auto-generated from halflife2.fgd, line 3065.
    Battery recharger
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_recharge", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "parentname", "origin", "_minlight", "spawnflags"])


class FuncTank(Entity):
    """
    Auto-generated from halflife2.fgd, line 3280.
    Brush Gun Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tank", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # Ammo type : TODO: Replace this filler.
        self.ammotype = "" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "ammotype", "spawnflags"])


class FuncTankCombineCannon(Entity):
    """
    Auto-generated from halflife2.fgd, line 3353.
    Combine sentry cannon
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tank_combine_cannon", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # Ammo type : TODO: Replace this filler.
        self.ammotype = "" 

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "ammotype"])


class FuncTankairboatgun(Entity):
    """
    Auto-generated from halflife2.fgd, line 3321.
    Airboat Gun Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tankairboatgun", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # Name of a prop_dynamic which is the airboat gun model : TODO: Replace this filler.
        self.airboat_gun_model = ""

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "airboat_gun_model"])


class FuncTankapcrocket(Entity):
    """
    Auto-generated from halflife2.fgd, line 3326.
    APC Rocket Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tankapcrocket", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # Projectile speed : Speed the rocket will travel at.
        self.rocketspeed = 800 
        # Burst shot count : Number of missiles to shoot in a burst
        self.burstcount = 10 

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "rocketspeed", "burstcount"])


class FuncTanklaser(Entity):
    """
    Auto-generated from halflife2.fgd, line 3310.
    Brush Laser Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tanklaser", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # env_laser Entity : TODO: Replace this filler.
        self.laserentity = ""

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "laserentity"])


class FuncTankmortar(Entity):
    """
    Auto-generated from halflife2.fgd, line 3335.
    Brush Mortar Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tankmortar", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # Explosion Magnitude : TODO: Replace this filler.
        self.iMagnitude = 100
        # Shell travel time : How long after the turret fires before the shell impacts
        self.firedelay = 2 
        # Firing start sound : Sound of the mortar firing
        self.firestartsound = "" 
        # Firing end sound : TODO: Replace this filler.
        self.fireendsound = ""
        # Incoming Shell Sound : Sound of the shell falling on the target
        self.incomingsound = "" 
        # Incoming warning time : How long before the shell impacts to play the warning sound
        self.warningtime = "1" 
        # Fire time variance : How much variability to add to fire rate (time +-)
        self.firevariance = "0" 

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "iMagnitude", "firedelay", "firestartsound", "fireendsound", "incomingsound", "warningtime", "firevariance"])


class FuncTankphyscannister(Entity):
    """
    Auto-generated from halflife2.fgd, line 3348.
    PhysCannister Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tankphyscannister", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # Barrel Volume : Name of a trigger the specifies the volume in which cannisters must be placed.
        self.barrel_volume = "" 

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "barrel_volume"])


class FuncTankpulselaser(Entity):
    """
    Auto-generated from halflife2.fgd, line 3299.
    Brush Pulse Laser
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tankpulselaser", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # Pulse Speed : How fast does pulse travel
        self.PulseSpeed = 1000 
        # Pulse Color : Color of the pulse
        self.PulseColor = "255 0 0" 
        # Pulse Width : Width of the pulse
        self.PulseWidth = "20" 
        # Pulse Life : (Seconds)  How long the pulse lasts
        self.PulseLife = 2 
        # Pulse Lag : (Seconds)  How far behind is pulse tail
        self.PulseLag = "0.05" 
        # Pulse Fire Sound : Sound played when pulse fires
        self.PulseFireSound = "" 

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "PulseSpeed", "PulseColor", "PulseWidth", "PulseLife", "PulseLag", "PulseFireSound"])


class FuncTankrocket(Entity):
    """
    Auto-generated from halflife2.fgd, line 3316.
    Brush Rocket Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_tankrocket", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Control Volume : Name of a trigger the specifies the volume in which a player must be to control this tank.
        self.control_volume = "" 
        # (Team) Master : TODO: Replace this filler.
        self.master = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw range : TODO: Replace this filler.
        self.yawrange = "180"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch range : TODO: Replace this filler.
        self.pitchrange = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "5"
        # Barrel Length : TODO: Replace this filler.
        self.barrel = "0"
        # Barrel Horizontal : TODO: Replace this filler.
        self.barrely = "0"
        # Barrel Vertical : TODO: Replace this filler.
        self.barrelz = "0"
        # Smoke Sprite : TODO: Replace this filler.
        self.spritesmoke = ""
        # Flash Sprite : TODO: Replace this filler.
        self.spriteflash = ""
        # Sprite scale : TODO: Replace this filler.
        self.spritescale = "1"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Rate of Fire : TODO: Replace this filler.
        self.firerate = "1"
        # Damage Per Bullet : If set to 0, it'll use the base weapon bullet's damage.
        self.bullet_damage = "0" 
        # Damage Per Bullet Vs Player : If set to 0, it'll use the Damage Per Bullet value.
        self.bullet_damage_vs_player = "0" 
        # Firing persistence : (Seconds) How long to keep firing at last known position after lose sight of target
        self.persistence = "1" 
        # Firing persistence2 : (Seconds) After lost enemy and persistence time has passed, how long to occasionally fire at enemy's last known position
        self.persistence2 = "0" 
        # Bullet accuracy : TODO: Replace this filler.
        self.firespread = 0 
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # Gun Base Attachment : If Parent is specified, this is the attachment point on the parent to aim from.
        self.gun_base_attach = "" 
        # Gun Barrel Attachment : If Parent is specified, this is the attachment point on the parent to fire from. If you specify this, you'll want to specify the Gun Base Attachment too.
        self.gun_barrel_attach = "" 
        # Gun Yaw Pose Param : If Parent + the Gun Pitch Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_yaw_pose_param = "" 
        # Gun Yaw Pose Center : The center yaw pose parameter of the gun on the parent
        self.gun_yaw_pose_center = "0" 
        # Gun Pitch Pose Param : If Parent + the Gun Yaw Pose Param is specified, then the gun itself will be invisible and the func_tank will steer a gun on the parent using the pose parameters.
        self.gun_pitch_pose_param = "" 
        # Gun Pitch Pose Center : The center pitch pose parameter of the gun on the parent
        self.gun_pitch_pose_center = "0" 
        # Ammunition Count : Only applies to player use. -1 = unlimited ammo.
        self.ammo_count = -1 
        # Lead Target : TODO: Replace this filler.
        self.LeadTarget = "No" 
        # NPC Man Point : Point where NPC must stand to man this func_tank.
        self.npc_man_point = "" 
        # Post-NPC Attack Grace Period : If specified, NPC's manning this func tank won't fire at the player, after firing at a non-player, for this amount of time.
        self.playergraceperiod = "0" 
        # Ignore Grace Upto : The player grace period is ignored if the player's under this distance from the func_tank.
        self.ignoregraceupto = "768" 
        # Player Lock Time : The tank must have the player as a target for this amount of time before it's allowed to fire.
        self.playerlocktimebeforefire = "0" 
        # Effect Handling : TODO: Replace this filler.
        self.effecthandling = 0 

        # Projectile speed : Speed the rocket will travel at.
        self.rocketspeed = 800 

        self.auto_properties.extend(["targetname", "parentname", "origin", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "globalname", "disableshadows", "spawnflags", "control_volume", "master", "yawrate", "yawrange", "yawtolerance", "pitchrate", "pitchrange", "pitchtolerance", "barrel", "barrely", "barrelz", "spritesmoke", "spriteflash", "spritescale", "rotatestartsound", "rotatesound", "rotatestopsound", "firerate", "bullet_damage", "bullet_damage_vs_player", "persistence", "persistence2", "firespread", "minRange", "maxRange", "_minlight", "gun_base_attach", "gun_barrel_attach", "gun_yaw_pose_param", "gun_yaw_pose_center", "gun_pitch_pose_param", "gun_pitch_pose_center", "ammo_count", "LeadTarget", "npc_man_point", "playergraceperiod", "ignoregraceupto", "playerlocktimebeforefire", "effecthandling", "rocketspeed"])


class FuncVehicleclip(Entity):
    """
    Auto-generated from halflife2.fgd, line 3087.
    Vehicle Clip
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "func_vehicleclip", vmf_map)

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 


        self.auto_properties.extend(["parentname", "targetname", "globalname"])


class GenericActor(Entity):
    """
    Auto-generated from halflife2.fgd, line 1958.
    Generic Actor NPC
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "generic_actor", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Model : TODO: Replace this filler.
        self.model = ""
        # Hull type : TODO: Replace this filler.
        self.hull_name = "Human" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "parentname", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "disableshadows", "model", "hull_name"])


class GrenadeHelicopter(Entity):
    """
    Auto-generated from halflife2.fgd, line 1532.
    Helicopter bomb
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "grenade_helicopter", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "parentname", "spawnflags"])


class InfoApcMissileHint(Entity):
    """
    Auto-generated from halflife2.fgd, line 4329.
    Something that helps APC missiles guide. If the missile can hit the associated target entitybetween the time it takes the current enemy to enter + leave the hint, then the missile will guide to the entity.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "info_apc_missile_hint", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Target Entity : The entity that the missile will guide towards if the conditions are met.
        self.target = "" 

        self.auto_properties.extend(["targetname", "origin", "angles", "StartDisabled", "target"])


class InfoDarknessmodeLightsource(Entity):
    """
    Auto-generated from halflife2.fgd, line 1889.
    
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "info_darknessmode_lightsource", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Light Radius : The radius around this lightsource in which Alyx will be able to see enemies.
        self.LightRadius = "256.0" 

        self.auto_properties.extend(["targetname", "StartDisabled", "LightRadius"])


class InfoRadarTarget(Entity):
    """
    Auto-generated from halflife2.fgd, line 443.
    Jalopy Radar Beacon
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "info_radar_target", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Effective Radius : How close the Jalopy must be to detect this beacon. If this radius is -1, the range is infinite
        self.radius = 6000 
        # Type of target : TODO: Replace this filler.
        self.type = 0 
        # Mode : TODO: Replace this filler.
        self.mode = 0 

        self.auto_properties.extend(["targetname", "parentname", "StartDisabled", "radius", "type", "mode"])


class InfoSnipertarget(Entity):
    """
    Auto-generated from halflife2.fgd, line 467.
    Sniper Target
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "info_snipertarget", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Sweep to speed : TODO: Replace this filler.
        self.speed = 2
        # Group Name : TODO: Replace this filler.
        self.groupname = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "parentname", "speed", "groupname", "spawnflags"])


class InfoTargetGunshipcrash(Entity):
    """
    Auto-generated from halflife2.fgd, line 1390.
    Gunship Crash Target
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "info_target_gunshipcrash", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""


        self.auto_properties.extend(["targetname", "parentname"])


class InfoTargetHelicopterCrash(Entity):
    """
    Auto-generated from halflife2.fgd, line 1386.
    Helicopter Crash Target
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "info_target_helicopter_crash", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""


        self.auto_properties.extend(["targetname", "parentname"])


class InfoTargetVehicleTransition(Entity):
    """
    Auto-generated from halflife2.fgd, line 463.
    Vehicle Transition Point
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "info_target_vehicle_transition", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 


        self.auto_properties.extend(["targetname", "angles", "StartDisabled"])


class InfoTeleporterCountdown(Entity):
    """
    Auto-generated from halflife2.fgd, line 4521.
    Countdown timer for the teleporter. The status of the teleporter will appear on vgui_screen entities whose panel is 'teleport_countdown_screen'.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "info_teleporter_countdown", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""


        self.auto_properties.extend(["targetname"])


class ItemAmmo357(Entity):
    """
    Auto-generated from halflife2.fgd, line 3433.
    Box of 357 ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_357", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmo357Large(Entity):
    """
    Auto-generated from halflife2.fgd, line 3434.
    Large Box of 357 ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_357_large", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoAr2(Entity):
    """
    Auto-generated from halflife2.fgd, line 3431.
    Box of AR2 ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_ar2", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoAr2Altfire(Entity):
    """
    Auto-generated from halflife2.fgd, line 3442.
    AR2 Alt-fire Round
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_ar2_altfire", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoAr2Large(Entity):
    """
    Auto-generated from halflife2.fgd, line 3432.
    Large Box of AR2 ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_ar2_large", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoCrate(Entity):
    """
    Auto-generated from halflife2.fgd, line 3451.
    Ammo Crate
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_crate", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # Ammo Type : TODO: Replace this filler.
        self.AmmoType = 0 

        self.auto_properties.extend(["targetname", "angles", "fademindist", "fademaxdist", "fadescale", "AmmoType"])


class ItemAmmoCrossbow(Entity):
    """
    Auto-generated from halflife2.fgd, line 3435.
    Box of Crossbow ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_crossbow", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoPistol(Entity):
    """
    Auto-generated from halflife2.fgd, line 3427.
    Box of Pistol ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_pistol", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoPistolLarge(Entity):
    """
    Auto-generated from halflife2.fgd, line 3428.
    Large Box of Pistol ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_pistol_large", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoSmg1(Entity):
    """
    Auto-generated from halflife2.fgd, line 3429.
    Box of SMG1 ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_smg1", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoSmg1Grenade(Entity):
    """
    Auto-generated from halflife2.fgd, line 3438.
    SMG1 Grenade
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_smg1_grenade", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemAmmoSmg1Large(Entity):
    """
    Auto-generated from halflife2.fgd, line 3430.
    Large Box of SMG1 ammo
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_ammo_smg1_large", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemBattery(Entity):
    """
    Auto-generated from halflife2.fgd, line 3439.
    HEV battery
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_battery", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemBoxBuckshot(Entity):
    """
    Auto-generated from halflife2.fgd, line 3436.
    Box Buckshot
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_box_buckshot", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemDynamicResupply(Entity):
    """
    Auto-generated from halflife2.fgd, line 3393.
    A dynamic item. When the player enters the PVS of this entity, it will determine the item most needed by the player, spawn one of those items, and remove itself. To determine which item the player most needs, it calculates which of the Desired Health/Armor/Ammo ratios the player is farthest from.\n\nIf the player is above all the desired levels, then no item will be spawned, unless this item_dynamic_resupply was created by an item_item_crate. In that case, a random piece of ammo used by a weapon, that the player has, will be spawned. If the 'Fallback to Health Vial' spawnflag is set, a health vial will be spawned instead of the ammo.\n\nBy default, the item_dynamic_resupply uses the values inside the Master resupply, instead of using it's own values. This makes it easy to tweak the desired loadout of many resupplies. The BecomeMaster input allows you to switch Masters dynamically as the level progresses.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_dynamic_resupply", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Desired Health Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max health.
        self.DesiredHealth = "1" 
        # Desired Armor Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max armor.
        self.DesiredArmor = "0.3" 
        # Desired Pistol Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoPistol = "0.5" 
        # Desired SMG1 Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoSMG1 = "0.5" 
        # Desired SMG1 Grenade Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoSMG1_Grenade = "0.1" 
        # Desired AR2 Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoAR2 = "0.4" 
        # Desired Shotgun Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoBuckshot = "0.5" 
        # Desired RPG Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoRPG_Round = "0" 
        # Desired Grenade Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoGrenade = "0.1" 
        # Desired 357 Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmo357 = "0" 
        # Desired Crossbow Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoCrossbow = "0" 
        # Desired AR2 Alt-fire Ammo Ratio : A ratio from 0 to 1. Attempt to fill the player up to this percentage of his max ammo carrying capacity.
        self.DesiredAmmoAR2_AltFire = "0" 

        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags", "spawnflags", "DesiredHealth", "DesiredArmor", "DesiredAmmoPistol", "DesiredAmmoSMG1", "DesiredAmmoSMG1_Grenade", "DesiredAmmoAR2", "DesiredAmmoBuckshot", "DesiredAmmoRPG_Round", "DesiredAmmoGrenade", "DesiredAmmo357", "DesiredAmmoCrossbow", "DesiredAmmoAR2_AltFire"])


class ItemHealthcharger(Entity):
    """
    Auto-generated from halflife2.fgd, line 3504.
    Health Charger
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_healthcharger", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""

        self.auto_properties.extend(["targetname", "angles", "fademindist", "fademaxdist", "fadescale", "_minlight"])


class ItemHealthkit(Entity):
    """
    Auto-generated from halflife2.fgd, line 3440.
    Small Health Kit
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_healthkit", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemHealthvial(Entity):
    """
    Auto-generated from halflife2.fgd, line 3441.
    Personal Health Kit
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_healthvial", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemItemCrate(Entity):
    """
    Auto-generated from halflife2.fgd, line 3472.
    Item Crate
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_item_crate", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Model Scale : A multiplier for the size of the model.
        self.modelscale = "1.0" 
        # Disable Shadows : Used to disable dynamic shadows on this entity.
        self.disableshadows = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Explosion Damage : If non-zero, when this entity breaks it will create an explosion that causes the specified amount of damage. See also 'Explosion Radius'.
        self.ExplodeDamage = 0 
        # Explosion Radius : If non-zero, when this entity breaks it will create an explosion with a radius of the specified amount. See also 'Explosion Damage'.
        self.ExplodeRadius = 0 
        # Performance Mode : Used to limit the amount of gibs produced when this entity breaks, for performance reasons.
        self.PerformanceMode = 0 
        # Break Model Message : If set, will use this break model message instead of the normal break behavior.
        self.BreakModelMessage = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Pressure Delay : Delay, in seconds, after 'broken' by pressure before breaking apart (allows for sound to play before breaking apart).
        self.pressuredelay = 0 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Min Damage to Hurt : The prop will ignore any damage events if the damage is less than this amount.
        self.minhealthdmg = 0 
        # Shadow Cast Distance : Use this to override how far this object casts shadows. 0 = default distance.
        self.shadowcastdist = 0 
        # Physics Impact Damage Scale : Scales damage energy when this object is hit by a physics object. NOTE: 0 means this feature is disabled for backwards compatibility.\nSet to 1.0 for materials as strong as flesh, smaller numbers indicate stronger materials.
        self.physdamagescale = "0.1" 
        # Impact damage type : TODO: Replace this filler.
        self.Damagetype = 0 
        # Damaging it Doesn't Push It : Used to determine whether or not damage should cause the brush to move.
        self.nodamageforces = 0 
        # Scale Factor For Inertia : Scales the angular mass of an object. Used to hack angular damage and collision response.
        self.inertiaScale = "1.0" 
        # Mass Scale : A scale multiplier for the object's mass.
        self.massScale = "0" 
        # Override Parameters : A list of physics key/value pairs that are usually in a physics prop .qc file. Format is 'key,value,key,value,etc'.
        self.overridescript = "" 
        # Health Level to Override Motion : If specified, this object will start motion disabled. Once its health has dropped below this specified amount, it will enable motion.
        self.damagetoenablemotion = 0 
        # Physics Impact Force to Override Motion : If specified, this object will start motion disabled. Any impact that imparts a force greater than this value on the physbox will enable motion.
        self.forcetoenablemotion = 0 
        # Sound to make when punted : TODO: Replace this filler.
        self.puntsound = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # Crate Contains : TODO: Replace this filler.
        self.CrateType = 0 
        # Crate Appearance : TODO: Replace this filler.
        self.CrateAppearance = 0 
        # Item Type : Class name of the entity to spawn when the crate is broken
        self.ItemClass = "item_dynamic_resupply" 
        # Item Count : Number of items to emit upon breakage
        self.ItemCount = 1 
        # Specific resupply : If item type is item_dynamic_resupply, specify a specific one to use instead of the master
        self.SpecificResupply = "" 

        self.auto_properties.extend(["targetname", "globalname", "angles", "model", "skin", "modelscale", "disableshadows", "targetname", "damagefilter", "disableshadows", "ExplodeDamage", "ExplodeRadius", "PerformanceMode", "BreakModelMessage", "spawnflags", "pressuredelay", "mindxlevel", "maxdxlevel", "fademindist", "fademaxdist", "fadescale", "spawnflags", "minhealthdmg", "shadowcastdist", "physdamagescale", "Damagetype", "nodamageforces", "inertiaScale", "massScale", "overridescript", "damagetoenablemotion", "forcetoenablemotion", "puntsound", "targetname", "angles", "damagefilter", "fademindist", "fademaxdist", "fadescale", "CrateType", "CrateAppearance", "ItemClass", "ItemCount", "SpecificResupply"])


class ItemRpgRound(Entity):
    """
    Auto-generated from halflife2.fgd, line 3437.
    RPG Round
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_rpg_round", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags"])


class ItemSuit(Entity):
    """
    Auto-generated from halflife2.fgd, line 3443.
    HEV Suit
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_suit", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "angles", "disableshadows", "fademindist", "fademaxdist", "fadescale", "spawnflags", "spawnflags"])


class ItemSuitcharger(Entity):
    """
    Auto-generated from halflife2.fgd, line 3514.
    Battery recharger
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "item_suitcharger", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # Minimum light level : TODO: Replace this filler.
        self._minlight = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "angles", "fademindist", "fademaxdist", "fadescale", "_minlight", "spawnflags"])


class LogicAchievement(Entity):
    """
    Auto-generated from halflife2.fgd, line 4004.
    Sends an achievement system related event from the map to the achievement system.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "logic_achievement", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Achievement Event : Named event is sent to the achievement system when this entity receives a 'FireEvent' input.
        self.AchievementEvent = 0 

        self.auto_properties.extend(["targetname", "StartDisabled", "AchievementEvent"])


class LogicChoreographedScene(Entity):
    """
    Auto-generated from halflife2.fgd, line 3753.
    Manages a choreographed scene of one or more actors.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "logic_choreographed_scene", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Scene file : TODO: Replace this filler.
        self.SceneFile = ""
        # Target 1 : TODO: Replace this filler.
        self.target1 = ""
        # Target 2 : TODO: Replace this filler.
        self.target2 = ""
        # Target 3 : TODO: Replace this filler.
        self.target3 = ""
        # Target 4 : TODO: Replace this filler.
        self.target4 = ""
        # Target 5 : TODO: Replace this filler.
        self.target5 = ""
        # Target 6 : TODO: Replace this filler.
        self.target6 = ""
        # Target 7 : TODO: Replace this filler.
        self.target7 = ""
        # Target 8 : TODO: Replace this filler.
        self.target8 = ""
        # If an Actor is talking... : What to do if an actor this scene needs is already talking when this scene is told to start.
        self.busyactor = 1 
        # On player death : What should this entity do if the player dies
        self.onplayerdeath = 0 

        self.auto_properties.extend(["targetname", "SceneFile", "target1", "target2", "target3", "target4", "target5", "target6", "target7", "target8", "busyactor", "onplayerdeath"])


class LogicSceneListManager(Entity):
    """
    Auto-generated from halflife2.fgd, line 3818.
    Manages a list of logic_choreographed_scene entities. Store choreo scenes in them in order that they will be played by other inputs. Whenever a scene plays, the manager will remove all scenes before that one in the list. The name of another logic_scene_list_manager can be entered in a slot instead of an invididual scene, which will cause all scenes in that manager to be removed when a later scene in this list is played.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "logic_scene_list_manager", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Scene 1 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene0 = "" 
        # Scene 2 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene1 = "" 
        # Scene 3 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene2 = "" 
        # Scene 4 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene3 = "" 
        # Scene 5 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene4 = "" 
        # Scene 6 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene5 = "" 
        # Scene 7 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene6 = "" 
        # Scene 8 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene7 = "" 
        # Scene 9 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene8 = "" 
        # Scene 10 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene9 = "" 
        # Scene 11 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene10 = "" 
        # Scene 12 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene11 = "" 
        # Scene 13 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene12 = "" 
        # Scene 14 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene13 = "" 
        # Scene 15 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene14 = "" 
        # Scene 16 : The name of a logic_choreographed_scene, or logic_scene_list_manager.
        self.scene15 = "" 

        self.auto_properties.extend(["targetname", "scene0", "scene1", "scene2", "scene3", "scene4", "scene5", "scene6", "scene7", "scene8", "scene9", "scene10", "scene11", "scene12", "scene13", "scene14", "scene15"])


class MonsterGeneric(Entity):
    """
    Auto-generated from halflife2.fgd, line 1947.
    Generic Script NPC
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "monster_generic", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Model : TODO: Replace this filler.
        self.model = ""
        # Body : TODO: Replace this filler.
        self.body = 0

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "disableshadows", "spawnflags", "model", "body"])


class NpcAdvisor(Entity):
    """
    Auto-generated from halflife2.fgd, line 1077.
    An adorable sluglike alien with benevolent psychic powers.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_advisor", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # World model : TODO: Replace this filler.
        self.model = "models/advisor.mdl"
        # Levitation Area : Trigger volume inside which levitated objects reside
        self.levitationarea = "" 
        # Levitation Goal Bottom : Objects will levitate at least this high
        self.levitategoal_bottom = "" 
        # Levitation Goal Top : Objects will levitate at least this high
        self.levitategoal_top = "" 
        # Staging Position Names : All entities with this name will be considered staging positions for the throw behavior.
        self.staging_ent_names = "" 
        # Priority Name For Grabbing : If any entities named this are present, the advisor will preferentially pick them up for throwing at the player.
        self.priority_grab_name = "" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "model", "levitationarea", "levitategoal_bottom", "levitategoal_top", "staging_ent_names", "priority_grab_name"])


class NpcAlyx(Entity):
    """
    Auto-generated from halflife2.fgd, line 1811.
    Alyx
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_alyx", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Always transition : If yes, this NPC will always teleport to a Player Squad Transition Point if they're not within the trigger_transition volume.
        self.AlwaysTransition = "No" 
        # Prevent picking up weapons? : If yes, this NPC will NOT be allowed to pick up weapons they find on the ground.
        self.DontPickupWeapons = "No" 
        # Is this a vital ally? : If yes, this NPC will cause the game to end if killed.
        self.GameEndAlly = "No" 

        # World model : TODO: Replace this filler.
        self.model = "models/alyx.mdl"
        # Weapons : TODO: Replace this filler.
        self.additionalequipment = "weapon_alyxgun" 
        # Prevent picking up weapons? : If yes, this NPC will NOT be allowed to pick up weapons they find on the ground.
        self.DontPickupWeapons = "1" 
        # Should alyx have her EMP? : TODO: Replace this filler.
        self.ShouldHaveEMP = "1" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "parentname", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "AlwaysTransition", "DontPickupWeapons", "GameEndAlly", "model", "additionalequipment", "DontPickupWeapons", "ShouldHaveEMP"])


class NpcAntlion(Entity):
    """
    Auto-generated from halflife2.fgd, line 499.
    Antlions will run away from ai_sound, type: Thumper.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_antlion", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Start Burrowed : TODO: Replace this filler.
        self.startburrowed = "No" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Alert Radius : TODO: Replace this filler.
        self.radius = 256
        # Distance until eluded : Only available when Burrow When Eluded is flagged
        self.eludedist = 1024 
        # Ignore Bugbait : TODO: Replace this filler.
        self.ignorebugbait = "No" 
        # Suppress unburrow effects : TODO: Replace this filler.
        self.unburroweffects = "No" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "startburrowed", "spawnflags", "radius", "eludedist", "ignorebugbait", "unburroweffects"])


class NpcAntlionGrub(Entity):
    """
    Auto-generated from halflife2.fgd, line 4629.
    Antlion grub
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_antlion_grub", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "angles", "fademindist", "fademaxdist", "fadescale", "globalname", "spawnflags"])


class NpcAntlionTemplateMaker(Entity):
    """
    Auto-generated from halflife2.fgd, line 2004.
    Antlion Template Maker
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_antlion_template_maker", vmf_map)

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Num. of NPCs : Number of NPCs that will spawn before this spawner is exhausted.
        self.MaxNPCCount = 1 
        # Frequency : How often (in seconds) a new NPC will be spawned. If set to -1, a new NPC will be made when the last NPC dies.
        self.SpawnFrequency = "5" 
        # Max Live NPCs : Maximum number of live children allowed at any one time (new ones will not be made until one dies). If set to -1, no limit is applied.
        self.MaxLiveChildren = 5 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Name of template NPC : TODO: Replace this filler.
        self.TemplateName = ""
        # Spawn on Hint Group : If specified, children will spawn on a hint node from this group, nearest the target.
        self.spawngroup = "" 
        # Spawn radius : Target must be within this distance of any node in the hint group specified above. If the target is outside the radius, no NPC will spawn.
        self.spawnradius = 512 
        # Spawn target : Targetname of the entity to try and spawn near.
        self.spawntarget = "" 
        # Fight target : Targetname of an entity used as a goal for the children to fight to.
        self.fighttarget = "" 
        # Follow target : Targetname of an entity used as a goal for the children to follow.
        self.followtarget = "" 
        # Vehicle Spawn Distance : This is a modifier of the current spawn distance. Spawn distance on a vehicle is based on speed, so this is just a modifier for it.
        self.vehicledistance = "1" 
        # Random Worker Spawn Rate : Percentage chance that a spawned antlion will be a worker. (0 = no chance, 1 = 100% chance)
        self.workerspawnrate = "0" 
        # Ignore Bugbait : TODO: Replace this filler.
        self.ignorebugbait = "0" 
        # Initial antlions in the pool. : Number of antlions in the pool at map start.
        self.pool_start = 0 
        # Max antlions in the pool. : Maximum number of antlions allowed in the pool. If 0, pool behavior is turned off.
        self.pool_max = 0 
        # Pool regen amount. : This is the number of antlions added to the pool every time it regenerates.
        self.pool_regen_amount = 0 
        # Pool regen time. : Time interval between pool regeneration ticks.
        self.pool_regen_time = 0 
        # Create Spore effect : TODO: Replace this filler.
        self.createspores = "0" 

        self.auto_properties.extend(["angles", "targetname", "angles", "StartDisabled", "StartDisabled", "spawnflags", "MaxNPCCount", "SpawnFrequency", "MaxLiveChildren", "spawnflags", "TemplateName", "spawngroup", "spawnradius", "spawntarget", "fighttarget", "followtarget", "vehicledistance", "workerspawnrate", "ignorebugbait", "pool_start", "pool_max", "pool_regen_amount", "pool_regen_time", "createspores"])


class NpcAntlionguard(Entity):
    """
    Auto-generated from halflife2.fgd, line 544.
    Antlion Guard
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_antlionguard", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Burrowed : TODO: Replace this filler.
        self.startburrowed = "No" 
        # Allow Bark : TODO: Replace this filler.
        self.allowbark = "No" 
        # Cavern Guard Model and Texture : TODO: Replace this filler.
        self.cavernbreed = "No" 
        # Use inside-Cavern Behavior : TODO: Replace this filler.
        self.incavern = "No" 
        # Shove Targets : TODO: Replace this filler.
        self.shovetargets = ""

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "startburrowed", "allowbark", "cavernbreed", "incavern", "shovetargets"])


class NpcApcdriver(Entity):
    """
    Auto-generated from halflife2.fgd, line 346.
    NPC used to drive an APC vehicle.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_apcdriver", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Vehicle to drive : TODO: Replace this filler.
        self.vehicle = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Maxspeed (percentage of vehicle's maxspeed). : TODO: Replace this filler.
        self.drivermaxspeed = 1
        # MinSpeed (percentage of vehicle's maxspeed). : TODO: Replace this filler.
        self.driverminspeed = 0

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "vehicle", "spawnflags", "spawnflags", "drivermaxspeed", "driverminspeed"])


class NpcBarnacle(Entity):
    """
    Auto-generated from halflife2.fgd, line 1331.
    Barnacle
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_barnacle", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Rest Distance : Distance above the ground that the tongue should lie when the barnacle is at rest
        self.RestDist = 16 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "fademindist", "fademaxdist", "fadescale", "spawnflags", "RestDist"])


class NpcBarney(Entity):
    """
    Auto-generated from halflife2.fgd, line 898.
    Barney
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_barney", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Always transition : If yes, this NPC will always teleport to a Player Squad Transition Point if they're not within the trigger_transition volume.
        self.AlwaysTransition = "No" 
        # Prevent picking up weapons? : If yes, this NPC will NOT be allowed to pick up weapons they find on the ground.
        self.DontPickupWeapons = "No" 
        # Is this a vital ally? : If yes, this NPC will cause the game to end if killed.
        self.GameEndAlly = "No" 

        # Weapons : TODO: Replace this filler.
        self.additionalequipment = "weapon_pistol" 
        # Facial expression override : TODO: Replace this filler.
        self.ExpressionOverride = ""

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "AlwaysTransition", "DontPickupWeapons", "GameEndAlly", "additionalequipment", "ExpressionOverride"])


class NpcBlob(Entity):
    """
    Auto-generated from halflife2.fgd, line 195.
    Blob
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_blob", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcBreen(Entity):
    """
    Auto-generated from halflife2.fgd, line 1909.
    Dr Breen
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_breen", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # World model : TODO: Replace this filler.
        self.model = "models/breen.mdl"
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "model", "spawnflags"])


class NpcBullseye(Entity):
    """
    Auto-generated from halflife2.fgd, line 678.
    Bullseye
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_bullseye", vmf_map)

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Health : TODO: Replace this filler.
        self.health = 35
        # Minimum Angle : Angle from the bullseye required for bullseye to be a valid enemy
        self.minangle = "360" 
        # Minimum Distance : Distance from the bullseye required for bullseye to be a valid enemy
        self.mindist = "0" 
        # Autoaim Radius : Radius of autoaim influence. Use ent_autoaim <picker> to visualize.
        self.autoaimradius = "0" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["parentname", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "health", "minangle", "mindist", "autoaimradius", "spawnflags"])


class NpcCitizen(Entity):
    """
    Auto-generated from halflife2.fgd, line 773.
    Citizen
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_citizen", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Always transition : If yes, this NPC will always teleport to a Player Squad Transition Point if they're not within the trigger_transition volume.
        self.AlwaysTransition = "No" 
        # Prevent picking up weapons? : If yes, this NPC will NOT be allowed to pick up weapons they find on the ground.
        self.DontPickupWeapons = "No" 
        # Is this a vital ally? : If yes, this NPC will cause the game to end if killed.
        self.GameEndAlly = "No" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Weapons : TODO: Replace this filler.
        self.additionalequipment = "0" 
        # Ammo to Resupply (if spawnflag set) : TODO: Replace this filler.
        self.ammosupply = "SMG1" 
        # Amount of ammo to give : TODO: Replace this filler.
        self.ammoamount = 1
        # Type : TODO: Replace this filler.
        self.citizentype = "Default" 
        # Expression Type : TODO: Replace this filler.
        self.expressiontype = "Random" 
        # Model : TODO: Replace this filler.
        self.model = "models/humans/group01/male_01.mdl" 
        # Facial expression override : TODO: Replace this filler.
        self.ExpressionOverride = ""
        # Fire output when Nav is blocked? : TODO: Replace this filler.
        self.notifynavfailblocked = 0 
        # Permanent squad member? : A permanent squad member cannot leave the player's squad unless killed or explicity removed by entity I/O. Only use this if you're sure you want to override the default AI.
        self.neverleaveplayersquad = 0 
        # Deny command speech concept : If you want your citizens to squad with the player but not obey commands the player gives to move the squad, put a concept here and this citizen will speak the concept instead of obeying the order.
        self.denycommandconcept = "" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "parentname", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "AlwaysTransition", "DontPickupWeapons", "GameEndAlly", "spawnflags", "additionalequipment", "ammosupply", "ammoamount", "citizentype", "expressiontype", "model", "ExpressionOverride", "notifynavfailblocked", "neverleaveplayersquad", "denycommandconcept"])


class NpcClawscanner(Entity):
    """
    Auto-generated from halflife2.fgd, line 1655.
    Claw Scanner
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_clawscanner", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # SpotlightLength : TODO: Replace this filler.
        self.spotlightlength = 500
        # SpotlightWidth : TODO: Replace this filler.
        self.spotlightwidth = 50
        # SpotlightDisabled : TODO: Replace this filler.
        self.spotlightdisabled = 0 
        # Should inspect : TODO: Replace this filler.
        self.ShouldInspect = 1 
        # Only Inspect Players : TODO: Replace this filler.
        self.OnlyInspectPlayers = 0 
        # Never Inspect Players : TODO: Replace this filler.
        self.NeverInspectPlayers = 0 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "spotlightlength", "spotlightwidth", "spotlightdisabled", "ShouldInspect", "OnlyInspectPlayers", "NeverInspectPlayers"])


class NpcCombineCamera(Entity):
    """
    Auto-generated from halflife2.fgd, line 214.
    Combine security camera
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_combine_camera", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Inner radius : The camera will only lock onto enemies that are within the inner radius.
        self.innerradius = 300 
        # Outer radius : The camera will flash amber when enemies are within the outer radius, but outside the inner radius.
        self.outerradius = 450 
        # Min req'd damage for hurting camera : TODO: Replace this filler.
        self.minhealthdmg = 0
        # Default target : The default target is the entity that the camera follows when it has no other target to track.
        self.defaulttarget = "" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "innerradius", "outerradius", "minhealthdmg", "defaulttarget"])


class NpcCombineCannon(Entity):
    """
    Auto-generated from halflife2.fgd, line 209.
    Combine Cannon
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_combine_cannon", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Sight radius : Radius distance at which the cannon is able to aquire enemies.
        self.sightdist = 1024 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "sightdist"])


class NpcCombineS(Entity):
    """
    Auto-generated from halflife2.fgd, line 955.
    Combine Soldier
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_combine_s", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Waiting to Rappel? : If yes, this NPC spawns suspended in air and awaits a BeginRappel input. It will then spawn a zipline and slide down. When it hits the ground, NPC will cut away the line and try to move forward a few feet to make room for the next NPC. The NPC will not attempt to clear its landing space if it cannot do so by taking a few steps forward
        self.waitingtorappel = "No" 

        # Weapons : TODO: Replace this filler.
        self.additionalequipment = "weapon_smg1" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Number of Grenades : TODO: Replace this filler.
        self.NumGrenades = "5" 

        # Model : Regular Soldier
        self.model = "models/combine_soldier.mdl" 
        # Tactical Variant : TODO: Replace this filler.
        self.tacticalvariant = "0" 
        # Walk Easy : When true, will use a variety of more casual walking animations instead of the standard walk. For use in crowds. WARNING: this animation only has a north component. For use under very special circumstances only.
        self.usemarch = "0" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "waitingtorappel", "additionalequipment", "spawnflags", "NumGrenades", "model", "tacticalvariant", "usemarch"])


class NpcCombinedropship(Entity):
    """
    Auto-generated from halflife2.fgd, line 1396.
    Combine Dropship
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_combinedropship", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Initial Speed : Sets the helicopter's desired speed that it should try to reach as soon as it's spawned.
        self.InitialSpeed = "0" 
        # Target path_track : (Optional) The name of a path_track entity that this NPC will fly to after spawning.
        self.target = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Land target name : TODO: Replace this filler.
        self.LandTarget = ""
        # Crate Gun Range : If the dropship's carrying a crate with a gun on it, it'll only shoot targets within this range.
        self.GunRange = 2048 
        # Name of Rollermine Template : If this dropship drops any rollermines due to the 'DropMines' input being fired, it will use this template for the rollermines it creates. If left blank, ordinary rollermines will be dropped.
        self.RollermineTemplate = "" 
        # Name of Template NPC 1 : TODO: Replace this filler.
        self.NPCTemplate = ""
        # Name of Template NPC 2 : TODO: Replace this filler.
        self.NPCTemplate2 = ""
        # Name of Template NPC 3 : TODO: Replace this filler.
        self.NPCTemplate3 = ""
        # Name of Template NPC 4 : TODO: Replace this filler.
        self.NPCTemplate4 = ""
        # Name of Template NPC 5 : TODO: Replace this filler.
        self.NPCTemplate5 = ""
        # Name of Template NPC 6 : TODO: Replace this filler.
        self.NPCTemplate6 = ""
        # Name of dustoff point for NPC 1 : TODO: Replace this filler.
        self.Dustoff1 = ""
        # Name of dustoff point for NPC 2 : TODO: Replace this filler.
        self.Dustoff2 = ""
        # Name of dustoff point for NPC 3 : TODO: Replace this filler.
        self.Dustoff3 = ""
        # Name of dustoff point for NPC 4 : TODO: Replace this filler.
        self.Dustoff4 = ""
        # Name of dustoff point for NPC 5 : TODO: Replace this filler.
        self.Dustoff5 = ""
        # Name of dustoff point for NPC 6 : TODO: Replace this filler.
        self.Dustoff6 = ""
        # Name of the APC to drop : TODO: Replace this filler.
        self.APCVehicleName = ""
        # Invulnerable : TODO: Replace this filler.
        self.Invulnerable = 0 
        # Crate Type : TODO: Replace this filler.
        self.CrateType = 2 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "InitialSpeed", "target", "spawnflags", "spawnflags", "LandTarget", "GunRange", "RollermineTemplate", "NPCTemplate", "NPCTemplate2", "NPCTemplate3", "NPCTemplate4", "NPCTemplate5", "NPCTemplate6", "Dustoff1", "Dustoff2", "Dustoff3", "Dustoff4", "Dustoff5", "Dustoff6", "APCVehicleName", "Invulnerable", "CrateType"])


class NpcCombinegunship(Entity):
    """
    Auto-generated from halflife2.fgd, line 1350.
    Combine Gunship
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_combinegunship", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Initial Speed : Sets the helicopter's desired speed that it should try to reach as soon as it's spawned.
        self.InitialSpeed = "0" 
        # Target path_track : (Optional) The name of a path_track entity that this NPC will fly to after spawning.
        self.target = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "InitialSpeed", "target", "spawnflags", "spawnflags"])


class NpcCrabsynth(Entity):
    """
    Auto-generated from halflife2.fgd, line 1779.
    Crab Synth
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_crabsynth", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcCranedriver(Entity):
    """
    Auto-generated from halflife2.fgd, line 331.
    NPC used to drive cranes.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_cranedriver", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Vehicle to drive : TODO: Replace this filler.
        self.vehicle = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # Pause time before dropping a ForceDrop specified object. : TODO: Replace this filler.
        self.releasepause = 0

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "vehicle", "spawnflags", "releasepause"])


class NpcCrow(Entity):
    """
    Auto-generated from halflife2.fgd, line 601.
    Crow
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_crow", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Deaf? : TODO: Replace this filler.
        self.deaf = 0 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "deaf"])


class NpcCscanner(Entity):
    """
    Auto-generated from halflife2.fgd, line 1602.
    City Scanner
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_cscanner", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # SpotlightLength : TODO: Replace this filler.
        self.spotlightlength = 500
        # SpotlightWidth : TODO: Replace this filler.
        self.spotlightwidth = 50
        # SpotlightDisabled : TODO: Replace this filler.
        self.spotlightdisabled = 0 
        # Should inspect : TODO: Replace this filler.
        self.ShouldInspect = 1 
        # Only Inspect Players : TODO: Replace this filler.
        self.OnlyInspectPlayers = 0 
        # Never Inspect Players : TODO: Replace this filler.
        self.NeverInspectPlayers = 0 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "spotlightlength", "spotlightwidth", "spotlightdisabled", "ShouldInspect", "OnlyInspectPlayers", "NeverInspectPlayers"])


class NpcDog(Entity):
    """
    Auto-generated from halflife2.fgd, line 1927.
    d0g
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_dog", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcEli(Entity):
    """
    Auto-generated from halflife2.fgd, line 1899.
    Eli Vance
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_eli", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # World model : TODO: Replace this filler.
        self.model = "models/eli.mdl"

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "parentname", "model"])


class NpcEnemyfinder(Entity):
    """
    Auto-generated from halflife2.fgd, line 702.
    EnemyFinder
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_enemyfinder", vmf_map)

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # FieldOfView : How far to look (1.0 = straight ahead, 0.0 = +/- 90 degrees, -1.0 = all directions)
        self.FieldOfView = "0.2" 
        # Min Search Dist : TODO: Replace this filler.
        self.MinSearchDist = 0
        # Max Search Dist : TODO: Replace this filler.
        self.MaxSearchDist = 2048
        # Player pass issue time : Amount of time an enemy is hidden after which a 'free pass' on reaquire is granted
        self.freepass_timetotrigger = 0 
        # Player pass duration : After granted 'free pass', the amount of time a target is allowed before reaquire
        self.freepass_duration = 0 
        # Player pass move tolerance : After granted 'free pass', the distance the target is allowed to move before reaquire
        self.freepass_movetolerance = 120 
        # Player pass refill rate : After free pass begins expiring, how much the time the target gets back for every second they hide again
        self.freepass_refillrate = "0.5" 
        # Player pass peek time : How long targets in cover are allowed to peek without penalty
        self.freepass_peektime = 0 
        # Start On : TODO: Replace this filler.
        self.StartOn = 1 

        self.auto_properties.extend(["parentname", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "FieldOfView", "MinSearchDist", "MaxSearchDist", "freepass_timetotrigger", "freepass_duration", "freepass_movetolerance", "freepass_refillrate", "freepass_peektime", "StartOn"])


class NpcEnemyfinderCombinecannon(Entity):
    """
    Auto-generated from halflife2.fgd, line 735.
    EnemyFinder Specifically for use with func_tank_combine_cannon
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_enemyfinder_combinecannon", vmf_map)

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # FieldOfView : How far to look (1.0 = straight ahead, 0.0 = +/- 90 degrees, -1.0 = all directions)
        self.FieldOfView = "0.2" 
        # Min Search Dist : TODO: Replace this filler.
        self.MinSearchDist = 0
        # Max Search Dist : TODO: Replace this filler.
        self.MaxSearchDist = 2048
        # Name of entity to snap to : Since it can be hard to position these entities with respect to the func_tank brushwork, you can specify an entity name here and this enemyfinder will position itself at the center of that entity.
        self.SnapToEnt = "" 
        # Player pass issue time : Amount of time an enemy is hidden after which a 'free pass' on reaquire is granted
        self.freepass_timetotrigger = 0 
        # Player pass duration : After granted 'free pass', the amount of time a target is allowed before reaquire
        self.freepass_duration = 0 
        # Player pass move tolerance : After granted 'free pass', the distance the target is allowed to move before reaquire
        self.freepass_movetolerance = 120 
        # Player pass refill rate : After free pass begins expiring, how much the time the target gets back for every second they hide again
        self.freepass_refillrate = "0.5" 
        # Player pass peek time : How long targets in cover are allowed to peek without penalty
        self.freepass_peektime = 0 
        # Start On : TODO: Replace this filler.
        self.StartOn = 1 

        self.auto_properties.extend(["parentname", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "FieldOfView", "MinSearchDist", "MaxSearchDist", "SnapToEnt", "freepass_timetotrigger", "freepass_duration", "freepass_movetolerance", "freepass_refillrate", "freepass_peektime", "StartOn"])


class NpcFastzombie(Entity):
    """
    Auto-generated from halflife2.fgd, line 1568.
    Fast Zombie
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_fastzombie", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcFastzombieTorso(Entity):
    """
    Auto-generated from halflife2.fgd, line 1573.
    Fast Zombie Torso
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_fastzombie_torso", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcFisherman(Entity):
    """
    Auto-generated from halflife2.fgd, line 892.
    Fisherman
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_fisherman", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Facial expression override : TODO: Replace this filler.
        self.ExpressionOverride = ""

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "ExpressionOverride"])


class NpcGman(Entity):
    """
    Auto-generated from halflife2.fgd, line 1923.
    The G-Man
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_gman", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore"])


class NpcGrenadeFrag(Entity):
    """
    Auto-generated from halflife2.fgd, line 203.
    Hand Grenade
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_grenade_frag", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcHeadcrab(Entity):
    """
    Auto-generated from halflife2.fgd, line 657.
    Headcrab
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_headcrab", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Start burrowed : TODO: Replace this filler.
        self.startburrowed = "No" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "startburrowed", "spawnflags", "parentname"])


class NpcHeadcrabBlack(Entity):
    """
    Auto-generated from halflife2.fgd, line 663.
    Black Headcrab
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_headcrab_black", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Start burrowed : TODO: Replace this filler.
        self.startburrowed = "No" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "startburrowed", "spawnflags"])


class NpcHeadcrabFast(Entity):
    """
    Auto-generated from halflife2.fgd, line 660.
    Fast Headcrab
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_headcrab_fast", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Start burrowed : TODO: Replace this filler.
        self.startburrowed = "No" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "startburrowed", "spawnflags"])


class NpcHeliAvoidbox(Entity):
    """
    Auto-generated from halflife2.fgd, line 1556.
    Helicopter avoidance box
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_heli_avoidbox", vmf_map)

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["origin", "angles", "parentname", "spawnflags"])


class NpcHeliAvoidsphere(Entity):
    """
    Auto-generated from halflife2.fgd, line 1547.
    Helicopter avoidance sphere
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_heli_avoidsphere", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Radius : TODO: Replace this filler.
        self.radius = 128

        self.auto_properties.extend(["targetname", "parentname", "spawnflags", "radius"])


class NpcHeliNobomb(Entity):
    """
    Auto-generated from halflife2.fgd, line 1564.
    Helicopter bombing suppressor
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_heli_nobomb", vmf_map)

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""


        self.auto_properties.extend(["origin", "angles", "parentname"])


class NpcHelicopter(Entity):
    """
    Auto-generated from halflife2.fgd, line 1465.
    Helicopter
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_helicopter", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Initial Speed : Sets the helicopter's desired speed that it should try to reach as soon as it's spawned.
        self.InitialSpeed = "0" 
        # Target path_track : (Optional) The name of a path_track entity that this NPC will fly to after spawning.
        self.target = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Initial Speed : TODO: Replace this filler.
        self.InitialSpeed = "0"
        # Grace Period : Time in seconds the helicopter has to see the player before he starts shooting
        self.GracePeriod = "2.0" 
        # Patrol Speed : Speed at which the helicopter moves if he has no enemy.
        self.PatrolSpeed = "0" 
        # Non-combat (Do not precache gibs) : TODO: Replace this filler.
        self.noncombat = "No" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "InitialSpeed", "target", "spawnflags", "spawnflags", "InitialSpeed", "GracePeriod", "PatrolSpeed", "noncombat"])


class NpcHunter(Entity):
    """
    Auto-generated from halflife2.fgd, line 1040.
    A smaller, faster, strider that can pursue the player into buildings.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_hunter", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Strider to Follow : The name of the strider that this hunter should follow.
        self.FollowTarget = "" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "FollowTarget"])


class NpcHunterMaker(Entity):
    """
    Auto-generated from halflife2.fgd, line 1071.
    An entity that creates hunters. The NPCs it creates are clones of a template NPC.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_hunter_maker", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Num. of NPCs : Number of NPCs that will spawn before this spawner is exhausted.
        self.MaxNPCCount = 1 
        # Frequency : How often (in seconds) a new NPC will be spawned. If set to -1, a new NPC will be made when the last NPC dies.
        self.SpawnFrequency = "5" 
        # Max Live NPCs : Maximum number of live children allowed at any one time (new ones will not be made until one dies). If set to -1, no limit is applied.
        self.MaxLiveChildren = 5 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Name of template NPC : Template NPC that this maker should be creating clones of.
        self.TemplateName = "" 
        # Radius : Radius around this maker within which NPCs are to be placed. Spawned NPCs will try and find empty space within this radius to spawn.
        self.Radius = 256 
        # Name of Destination Group : If you'd like spawned NPCs to be created at an info_npc_spawn_destination entity, enter the name of that entity here. If you have more than one destination entity by that name, the Destination Criteria will be used to select one from the group.
        self.DestinationGroup = ""
        # Dest. Criterion: Visible to player? : Should the NPC try to spawn at a destination that the player can see? Only applicable if a Destination Group is being used.
        self.CriterionVisibility = 2 
        # Dest. Criterion: Distance to player? : Should the NPC try to spawn nearest to or farthest from the player's current location? Only applicable if a Destination Group is being used.
        self.CriterionDistance = 2 
        # Minimum spawn distance from player : The spawn destination node distance to the player will have to be further or equal than this value.
        self.MinSpawnDistance = 0 


        self.auto_properties.extend(["targetname", "angles", "StartDisabled", "StartDisabled", "spawnflags", "MaxNPCCount", "SpawnFrequency", "MaxLiveChildren", "spawnflags", "TemplateName", "Radius", "DestinationGroup", "CriterionVisibility", "CriterionDistance", "MinSpawnDistance"])


class NpcIchthyosaur(Entity):
    """
    Auto-generated from halflife2.fgd, line 634.
    Ichthyosaur. Not fully functional
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_ichthyosaur", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcKleiner(Entity):
    """
    Auto-generated from halflife2.fgd, line 1894.
    Kleiner
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_kleiner", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # World model : TODO: Replace this filler.
        self.model = "models/kleiner.mdl"

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "model"])


class NpcLauncher(Entity):
    """
    Auto-generated from halflife2.fgd, line 981.
    Launcher
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_launcher", vmf_map)

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start On : TODO: Replace this filler.
        self.StartOn = 0 
        # Missile Model : TODO: Replace this filler.
        self.MissileModel = "models/Weapons/wscanner_grenade.mdl"
        # Launch Sound : TODO: Replace this filler.
        self.LaunchSound = "npc/waste_scanner/grenade_fire.wav"
        # Fly Sound : TODO: Replace this filler.
        self.FlySound = "ambient/objects/machine2.wav"
        # Smoke Trail : TODO: Replace this filler.
        self.SmokeTrail = 1 
        # Launch Smoke : TODO: Replace this filler.
        self.LaunchSmoke = 1 
        # Launch Delay : When ON, how long to wait between each launch
        self.LaunchDelay = 8	    
        # Launch Speed : Initial launch speed of missile (in up direction)
        self.LaunchSpeed = 200   
        # Path Corner Name : Name of a path corner.  If set launches a pathfollowing missing, rather than a homing missile
        self.PathCornerName = "" 
        # Homing Speed : Speed to reach when homing
        self.HomingSpeed = 0	    
        # Homing Strength : How strong in homing effect (0-100)
        self.HomingStrength = 10    
        # Homing Delay : Number of seconds to delay before homing starts
        self.HomingDelay = 0 
        # Homing Ramp Up : Number of seconds it takes to reach full homing strength after homing delay
        self.HomingRampUp = "0.5" 
        # Homing Duration : Number of seconds the homing effect lasts
        self.HomingDuration = 5     
        # Homing Ramp Down : Number of seconds it takes homing effect to decay after homing duration
        self.HomingRampDown = "1.0" 
        # Gravity : Scale for effect of gravity. (1.0 = normal gravity)
        self.Gravity = "1.0" 
        # Min Range : Minimun distance a target can be to be attacked
        self.MinRange = 100   
        # Max Range : Maximum distance a target can be to be attacked
        self.MaxRange = 2048  
        # Spin Magnitude : Strength of spin in missile trajectory
        self.SpinMagnitude = 0	    
        # Spin Speed : How fast does the spin rotate through 360
        self.SpinSpeed = 0     
        # Damage : How much damage does each missile do
        self.Damage = 50    
        # DamageRadius : How far away from impact does the missle do damage
        self.DamageRadius = 200   

        self.auto_properties.extend(["parentname", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "StartOn", "MissileModel", "LaunchSound", "FlySound", "SmokeTrail", "LaunchSmoke", "LaunchDelay", "LaunchSpeed", "PathCornerName", "HomingSpeed", "HomingStrength", "HomingDelay", "HomingRampUp", "HomingDuration", "HomingRampDown", "Gravity", "MinRange", "MaxRange", "SpinMagnitude", "SpinSpeed", "Damage", "DamageRadius"])


class NpcMagnusson(Entity):
    """
    Auto-generated from halflife2.fgd, line 1904.
    Magnusson
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_magnusson", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # World model : TODO: Replace this filler.
        self.model = "models/magnusson.mdl"

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "model"])


class NpcMaker(Entity):
    """
    Auto-generated from halflife2.fgd, line 1980.
    NPC Maker
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_maker", vmf_map)

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Num. of NPCs : Number of NPCs that will spawn before this spawner is exhausted.
        self.MaxNPCCount = 1 
        # Frequency : How often (in seconds) a new NPC will be spawned. If set to -1, a new NPC will be made when the last NPC dies.
        self.SpawnFrequency = "5" 
        # Max Live NPCs : Maximum number of live children allowed at any one time (new ones will not be made until one dies). If set to -1, no limit is applied.
        self.MaxLiveChildren = 5 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Class name of spawned NPC : TODO: Replace this filler.
        self.NPCType = ""
        # Childrens' Name : TODO: Replace this filler.
        self.NPCTargetname = ""
        # Childrens' Squad Name : TODO: Replace this filler.
        self.NPCSquadname = ""
        # Childrens' Hint Group : TODO: Replace this filler.
        self.NPCHintGroup = ""
        # Weapons : TODO: Replace this filler.
        self.additionalequipment = "0" 

        self.auto_properties.extend(["angles", "targetname", "angles", "StartDisabled", "StartDisabled", "spawnflags", "MaxNPCCount", "SpawnFrequency", "MaxLiveChildren", "spawnflags", "NPCType", "NPCTargetname", "NPCSquadname", "NPCHintGroup", "additionalequipment"])


class NpcManhack(Entity):
    """
    Auto-generated from halflife2.fgd, line 1707.
    Manhack
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_manhack", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Ignore NPC Clip brushes : TODO: Replace this filler.
        self.ignoreclipbrushes = "0" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "ignoreclipbrushes"])


class NpcMetropolice(Entity):
    """
    Auto-generated from halflife2.fgd, line 1732.
    MetroPolice
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_metropolice", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Waiting to Rappel? : If yes, this NPC spawns suspended in air and awaits a BeginRappel input. It will then spawn a zipline and slide down. When it hits the ground, NPC will cut away the line and try to move forward a few feet to make room for the next NPC. The NPC will not attempt to clear its landing space if it cannot do so by taking a few steps forward
        self.waitingtorappel = "No" 

        # Weapons : TODO: Replace this filler.
        self.additionalequipment = "weapon_pistol" 
        # Number of Manhacks : TODO: Replace this filler.
        self.manhacks = 0 
        # Pistol starts drawn : TODO: Replace this filler.
        self.weapondrawn = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "waitingtorappel", "additionalequipment", "manhacks", "weapondrawn", "spawnflags"])


class NpcMissiledefense(Entity):
    """
    Auto-generated from halflife2.fgd, line 395.
    Missile Defense
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_missiledefense", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcMonk(Entity):
    """
    Auto-generated from halflife2.fgd, line 1783.
    Monk
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_monk", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # Weapons : TODO: Replace this filler.
        self.additionalequipment = "weapon_annabelle" 
        # Has Gun : TODO: Replace this filler.
        self.HasGun = 1 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "additionalequipment", "HasGun"])


class NpcMortarsynth(Entity):
    """
    Auto-generated from halflife2.fgd, line 1727.
    Mortar Synth
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_mortarsynth", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcMossman(Entity):
    """
    Auto-generated from halflife2.fgd, line 1919.
    Dr Mossman
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_mossman", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore"])


class NpcPigeon(Entity):
    """
    Auto-generated from halflife2.fgd, line 623.
    Pigeon
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_pigeon", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Deaf? : TODO: Replace this filler.
        self.deaf = 0 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "deaf"])


class NpcPoisonzombie(Entity):
    """
    Auto-generated from halflife2.fgd, line 1591.
    A bloated, disgusting, fluid-spurting zombie created by a poison headcrab.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_poisonzombie", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Crabs in nest : TODO: Replace this filler.
        self.crabcount = 3 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "crabcount"])


class NpcRollermine(Entity):
    """
    Auto-generated from halflife2.fgd, line 368.
    Roller Mine
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_rollermine", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Buried : TODO: Replace this filler.
        self.startburied = "No" 
        # Sight Distance : TODO: Replace this filler.
        self.uniformsightdist = 0 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "startburied", "uniformsightdist"])


class NpcSeagull(Entity):
    """
    Auto-generated from halflife2.fgd, line 612.
    Seagull
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_seagull", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Deaf? : TODO: Replace this filler.
        self.deaf = 0 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "deaf"])


class NpcSniper(Entity):
    """
    Auto-generated from halflife2.fgd, line 399.
    Sniper
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_sniper", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Patience Radius : Sniper will hold fire until the target is within this radius. After that, sniper will always attack, even if the target retreats outside of this radius
        self.radius = 0 
        # Initial Misses : How many times to miss a target on purpose before beginning to fire accurately.
        self.misses = 0 
        # Beam Brightness (0 to 255) : How bright the laser sight beam should be. (0 - 255)
        self.beambrightness = 100 
        # Shoot zombies in chest : If true, aim for chest instead of headcrab. Useful when at great distance and height.
        self.shootZombiesInChest = 0 
        # Bullet shield distance : Obsolete
        self.shielddistance = 64 
        # Bullet shield radius : Obsolete
        self.shieldradius = 48 
        # Paint interval : Sniper will keep a target painted for this many seconds before shooting. 'Faster Shooting' sniper flag multiplies this by 0.75.
        self.PaintInterval = 1 
        # Paint interval variance : When shooting at NPCs, a random number between 0 and this is added to PaintInterval for every shot.
        self.PaintIntervalVariance = "0.75" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "radius", "misses", "beambrightness", "shootZombiesInChest", "shielddistance", "shieldradius", "PaintInterval", "PaintIntervalVariance", "spawnflags"])


class NpcSpotlight(Entity):
    """
    Auto-generated from halflife2.fgd, line 1241.
    Spotlight
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_spotlight", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Health : TODO: Replace this filler.
        self.health = 100
        # YawRange : TODO: Replace this filler.
        self.YawRange = 90
        # PitchMin : TODO: Replace this filler.
        self.PitchMin = 35
        # PitchMax : TODO: Replace this filler.
        self.PitchMax = 50
        # IdleSpeed : TODO: Replace this filler.
        self.IdleSpeed = 2
        # AlertSpeed : TODO: Replace this filler.
        self.AlertSpeed = 5
        # SpotlightLength : TODO: Replace this filler.
        self.spotlightlength = 500
        # SpotlightWidth : TODO: Replace this filler.
        self.spotlightwidth = 50
        # Color (R G B) : TODO: Replace this filler.
        self.rendercolor = "255 255 255"

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "spawnflags", "health", "YawRange", "PitchMin", "PitchMax", "IdleSpeed", "AlertSpeed", "spotlightlength", "spotlightwidth", "rendercolor"])


class NpcStalker(Entity):
    """
    Auto-generated from halflife2.fgd, line 667.
    Stalker
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_stalker", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Beam Power : TODO: Replace this filler.
        self.BeamPower = "Low" 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "BeamPower"])


class NpcStrider(Entity):
    """
    Auto-generated from halflife2.fgd, line 1276.
    Strider
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_strider", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Model : Standard strider
        self.model = "models/combine_strider.mdl" 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Disable physics (reduce CPU) : TODO: Replace this filler.
        self.disablephysics = 0 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "model", "spawnflags", "disablephysics"])


class NpcTurretCeiling(Entity):
    """
    Auto-generated from halflife2.fgd, line 252.
    Combine Ceiling Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_turret_ceiling", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Model Scale : A multiplier for the size of the model.
        self.modelscale = "1.0" 
        # Disable Shadows : Used to disable dynamic shadows on this entity.
        self.disableshadows = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Min req'd damage for hurting turret : TODO: Replace this filler.
        self.minhealthdmg = 0

        self.auto_properties.extend(["targetname", "angles", "model", "skin", "modelscale", "disableshadows", "spawnflags", "minhealthdmg"])


class NpcTurretFloor(Entity):
    """
    Auto-generated from halflife2.fgd, line 276.
    Combine Floor Turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_turret_floor", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Skin Number : Which skin to use for this turret. Set to 0 to select randomly.
        self.SkinNumber = 0 

        self.auto_properties.extend(["targetname", "angles", "spawnflags", "SkinNumber"])


class NpcTurretGround(Entity):
    """
    Auto-generated from halflife2.fgd, line 244.
    Combine ground turret
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_turret_ground", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""



        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "parentname"])


class NpcVehicledriver(Entity):
    """
    Auto-generated from halflife2.fgd, line 319.
    NPC used to drive a target vehicle.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_vehicledriver", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Vehicle to drive : TODO: Replace this filler.
        self.vehicle = ""
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        # Maxspeed (percentage of vehicle's maxspeed). : TODO: Replace this filler.
        self.drivermaxspeed = 1
        # MinSpeed (percentage of vehicle's maxspeed). : TODO: Replace this filler.
        self.driverminspeed = 0

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "vehicle", "spawnflags", "drivermaxspeed", "driverminspeed"])


class NpcVortigaunt(Entity):
    """
    Auto-generated from halflife2.fgd, line 1206.
    Vortigaunt
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_vortigaunt", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Use Sentence : TODO: Replace this filler.
        self.UseSentence = ""
        # Un-Use Sentence : TODO: Replace this filler.
        self.UnUseSentence = ""
        # Don't Use Speech Semaphore : Friendly NPCs are not allowed to speak if another friendly NPC is speaking. In some cases we don't want speaking NPCs to prevent other NPCs from speaking (for instance, if there is a friendly NPC speaking for a long time on a monitor). To make this NPC not prevent other NPCs from talking, make it not grab the semaphore when it speaks.
        self.DontUseSpeechSemaphore = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 

        # Always transition : If yes, this NPC will always teleport to a Player Squad Transition Point if they're not within the trigger_transition volume.
        self.AlwaysTransition = "No" 
        # Prevent picking up weapons? : If yes, this NPC will NOT be allowed to pick up weapons they find on the ground.
        self.DontPickupWeapons = "No" 
        # Is this a vital ally? : If yes, this NPC will cause the game to end if killed.
        self.GameEndAlly = "No" 

        # World model : TODO: Replace this filler.
        self.model = "models/vortigaunt.mdl"
        # Allow Armor Recharging : TODO: Replace this filler.
        self.ArmorRechargeEnabled = 1 
        # Regenerate Health : Whether or not the vortigaunt will recover his own health over time like vital allies, while in combat
        self.HealthRegenerateEnabled = 0 

        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "UseSentence", "UnUseSentence", "DontUseSpeechSemaphore", "targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale", "AlwaysTransition", "DontPickupWeapons", "GameEndAlly", "model", "ArmorRechargeEnabled", "HealthRegenerateEnabled"])


class NpcZombie(Entity):
    """
    Auto-generated from halflife2.fgd, line 1578.
    Zombie
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_zombie", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcZombieTorso(Entity):
    """
    Auto-generated from halflife2.fgd, line 1582.
    Zombie Torso
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_zombie_torso", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class NpcZombine(Entity):
    """
    Auto-generated from halflife2.fgd, line 1586.
    Combine Soldier Zombie
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "npc_zombine", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Response Contexts : Response system context(s) for this entity. Format should be: 'key:value,key2:value2,etc'. When this entity speaks, the list of keys & values will be passed to the response rules system.
        self.ResponseContext = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Target Path Corner : If set, the name of a path corner entity that this NPC will walk to, after spawning.
        self.target = ""
        # Squad Name : NPCs that are in the same squad (i.e. have matching squad names) will share information about enemies, and will take turns attacking and covering each other.
        self.squadname = ""
        # Hint Group : Hint groups are used by NPCs to restrict their hint-node searching to a subset of the map's hint nodes. Only hint nodes with matching hint group names will be considered by this NPC.
        self.hintgroup = "" 
        # Hint Limit Nav : Limits NPC to using specified hint group for navigation requests, but does not limit local navigation.
        self.hintlimiting = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sleep State : Holds the NPC in stasis until specified condition. See also 'Wake Radius' and 'Wake Squad'.
        self.sleepstate = 0 
        # Wake Radius : Auto-wake if player within this distance
        self.wakeradius = 0 
        # Wake Squad : Wake all of the NPCs squadmates if the NPC is woken
        self.wakesquad = 0 
        # Enemy Filter : Filter by which to filter potential enemies
        self.enemyfilter = "" 
        # Ignore unseen enemies : Prefer visible enemies, regardless of distance or relationship priority
        self.ignoreunseenenemies = 0 
        # Physics Impact Damage Scale : Scales damage energy when this character is hit by a physics object. With a value of 0 the NPC will take no damage from physics.
        self.physdamagescale = "1.0" 


        self.auto_properties.extend(["targetname", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows", "damagefilter", "ResponseContext", "disableshadows", "target", "squadname", "hintgroup", "hintlimiting", "spawnflags", "sleepstate", "wakeradius", "wakesquad", "enemyfilter", "ignoreunseenenemies", "physdamagescale"])


class PathCorner(Entity):
    """
    Auto-generated from halflife2.fgd, line 3677.
    Generic path point
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "path_corner", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Next stop target : TODO: Replace this filler.
        self.target = ""
        # Wait here (secs) : TODO: Replace this filler.
        self.wait = 0
        # New Train Speed : TODO: Replace this filler.
        self.speed = 0
        # New Train rot. Speed : TODO: Replace this filler.
        self.yaw_speed = 0

        self.auto_properties.extend(["targetname", "angles", "spawnflags", "target", "wait", "speed", "yaw_speed"])


class PathCornerCrash(Entity):
    """
    Auto-generated from halflife2.fgd, line 3696.
    Helicopter Crash Path
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "path_corner_crash", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Next stop target : TODO: Replace this filler.
        self.target = ""

        self.auto_properties.extend(["targetname", "target"])


class PlayerLoadsaved(Entity):
    """
    Auto-generated from halflife2.fgd, line 3708.
    Load Auto-Saved game
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "player_loadsaved", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Fade Duration (seconds) : TODO: Replace this filler.
        self.duration = "2"
        # Hold Fade (seconds) : TODO: Replace this filler.
        self.holdtime = "0"
        # Fade Alpha : TODO: Replace this filler.
        self.renderamt = 255
        # Fade Color (R G B) : TODO: Replace this filler.
        self.rendercolor = "0 0 0"
        # Reload delay : TODO: Replace this filler.
        self.loadtime = "0"

        self.auto_properties.extend(["targetname", "duration", "holdtime", "renderamt", "rendercolor", "loadtime"])


class PlayerSpeedmod(Entity):
    """
    Auto-generated from halflife2.fgd, line 3728.
    Speeds up or slows down player velocity over time (slow mo/fast forward)
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "player_speedmod", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "spawnflags"])


class PlayerWeaponstrip(Entity):
    """
    Auto-generated from halflife2.fgd, line 3721.
    Strips player's weapons
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "player_weaponstrip", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""


        self.auto_properties.extend(["targetname"])


class PointAntlionRepellant(Entity):
    """
    Auto-generated from halflife2.fgd, line 2057.
    Antlion Repellant
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "point_antlion_repellant", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Repell radius : Antlions aren't allowed to be inside this radius
        self.repelradius = 512 

        self.auto_properties.extend(["targetname", "repelradius"])


class PointApcController(Entity):
    """
    Auto-generated from halflife2.fgd, line 4287.
    APC Controller
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "point_apc_controller", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Yaw rate : TODO: Replace this filler.
        self.yawrate = "30"
        # Yaw tolerance : TODO: Replace this filler.
        self.yawtolerance = "15"
        # Pitch rate : TODO: Replace this filler.
        self.pitchrate = "0"
        # Pitch tolerance : TODO: Replace this filler.
        self.pitchtolerance = "20"
        # Rotate Start Sound : TODO: Replace this filler.
        self.rotatestartsound = ""
        # Rotate Loop Sound : TODO: Replace this filler.
        self.rotatesound = ""
        # Rotate Stop Sound : TODO: Replace this filler.
        self.rotatestopsound = ""
        # Minmum target range : TODO: Replace this filler.
        self.minRange = "0"
        # Maximum target range : TODO: Replace this filler.
        self.maxRange = "0"
        # Name of entity I should follow/attack : TODO: Replace this filler.
        self.targetentityname = ""

        self.auto_properties.extend(["targetname", "angles", "spawnflags", "yawrate", "yawtolerance", "pitchrate", "pitchtolerance", "rotatestartsound", "rotatesound", "rotatestopsound", "minRange", "maxRange", "targetentityname"])


class PointBugbait(Entity):
    """
    Auto-generated from halflife2.fgd, line 3639.
    Bugbait sensor point
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "point_bugbait", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Start Enabled : TODO: Replace this filler.
        self.Enabled = 1 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sensor Radius : TODO: Replace this filler.
        self.radius = 512

        self.auto_properties.extend(["targetname", "Enabled", "spawnflags", "radius"])


class PointCamera(Entity):
    """
    Auto-generated from halflife2.fgd, line 4173.
    Camera
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "point_camera", vmf_map)

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Name : The name that other entities refer to this entity by.
        self.targetname = ""
        # FOV : Field of view in degrees
        self.FOV = 90 
        # Screen Aspect Ratio : TODO: Replace this filler.
        self.UseScreenAspectRatio = 0 
        # Fog Enable : TODO: Replace this filler.
        self.fogEnable = 0 
        # Fog Color : TODO: Replace this filler.
        self.fogColor = "0 0 0"
        # Fog Start : The near fog plane.
        self.fogStart = 2048 
        # Fog End : The far fog/clipping plane.
        self.fogEnd = 4096 
        # Fog Max Density [0..1] : The maximum fog density. 0=no fog, 1=full fog.
        self.fogMaxDensity = 1 

        self.auto_properties.extend(["parentname", "angles", "spawnflags", "targetname", "FOV", "UseScreenAspectRatio", "fogEnable", "fogColor", "fogStart", "fogEnd", "fogMaxDensity"])


class PointCombineBallLauncher(Entity):
    """
    Auto-generated from halflife2.fgd, line 178.
    Launches Combine balls.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "point_combine_ball_launcher", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Ball count : This is how many balls will be bouncing around inside the spawner
        self.ballcount = 3 
        # Min ball speed : The minimum speed of balls that fly in the spawner
        self.minspeed = "300.0" 
        # Max ball speed : The maximum speed of balls that fly in the spawner
        self.maxspeed = "600.0" 
        # Ball radius : The radius of the energy balls
        self.ballradius = "20.0" 
        # Ball Type : TODO: Replace this filler.
        self.balltype = "Combine Energy Ball 1" 
        # Ball Respawn Time : The energy balls respawn time
        self.ballrespawntime = "4.0f" 

        # Noise to launch direction : Noise in degrees added to the launch direction.
        self.launchconenoise = "0.0" 
        # Name of bullseye : If you select the Attach Bullseye spawnflag, you may specify a name here which will be given to the bullseye.
        self.bullseyename = "" 
        # Max number of bounces : Maximum number of bounces the balls are allowed to do before they are removed.
        self.maxballbounces = 8 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "origin", "angles", "globalname", "spawnflags", "ballcount", "minspeed", "maxspeed", "ballradius", "balltype", "ballrespawntime", "launchconenoise", "bullseyename", "maxballbounces", "spawnflags"])


class PointFleshEffectTarget(Entity):
    """
    Auto-generated from halflife2.fgd, line 4665.
    Flesh Effect Target
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "point_flesh_effect_target", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Radius : Radius of the effect when active
        self.radius = 8 

        self.auto_properties.extend(["targetname", "parentname", "radius"])


class PointPush(Entity):
    """
    Auto-generated from halflife2.fgd, line 4604.
    Point Push
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "point_push", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Enabled : TODO: Replace this filler.
        self.enabled = 1 
        # Magnitude : How strongly to push away from the center.
        self.magnitude = 100 
        # Radius : Radius to affect items in.
        self.radius = 128 
        # Inner radius : If not zero, the LOS is calculated from a point intersecting this sphere.
        self.inner_radius = "0" 

        self.auto_properties.extend(["targetname", "angles", "spawnflags", "enabled", "magnitude", "radius", "inner_radius"])


class PropCombineBall(Entity):
    """
    Auto-generated from halflife2.fgd, line 129.
    A prop that physically simulates as a single rigid body. It can be constrained to other physics objects using hinges or other constraints. It can also be configured to break when it takes enough damage.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_combine_ball", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Model Scale : A multiplier for the size of the model.
        self.modelscale = "1.0" 
        # Disable Shadows : Used to disable dynamic shadows on this entity.
        self.disableshadows = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Explosion Damage : If non-zero, when this entity breaks it will create an explosion that causes the specified amount of damage. See also 'Explosion Radius'.
        self.ExplodeDamage = 0 
        # Explosion Radius : If non-zero, when this entity breaks it will create an explosion with a radius of the specified amount. See also 'Explosion Damage'.
        self.ExplodeRadius = 0 
        # Performance Mode : Used to limit the amount of gibs produced when this entity breaks, for performance reasons.
        self.PerformanceMode = 0 
        # Break Model Message : If set, will use this break model message instead of the normal break behavior.
        self.BreakModelMessage = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Pressure Delay : Delay, in seconds, after 'broken' by pressure before breaking apart (allows for sound to play before breaking apart).
        self.pressuredelay = 0 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Min Damage to Hurt : The prop will ignore any damage events if the damage is less than this amount.
        self.minhealthdmg = 0 
        # Shadow Cast Distance : Use this to override how far this object casts shadows. 0 = default distance.
        self.shadowcastdist = 0 
        # Physics Impact Damage Scale : Scales damage energy when this object is hit by a physics object. NOTE: 0 means this feature is disabled for backwards compatibility.\nSet to 1.0 for materials as strong as flesh, smaller numbers indicate stronger materials.
        self.physdamagescale = "0.1" 
        # Impact damage type : TODO: Replace this filler.
        self.Damagetype = 0 
        # Damaging it Doesn't Push It : Used to determine whether or not damage should cause the brush to move.
        self.nodamageforces = 0 
        # Scale Factor For Inertia : Scales the angular mass of an object. Used to hack angular damage and collision response.
        self.inertiaScale = "1.0" 
        # Mass Scale : A scale multiplier for the object's mass.
        self.massScale = "0" 
        # Override Parameters : A list of physics key/value pairs that are usually in a physics prop .qc file. Format is 'key,value,key,value,etc'.
        self.overridescript = "" 
        # Health Level to Override Motion : If specified, this object will start motion disabled. Once its health has dropped below this specified amount, it will enable motion.
        self.damagetoenablemotion = 0 
        # Physics Impact Force to Override Motion : If specified, this object will start motion disabled. Any impact that imparts a force greater than this value on the physbox will enable motion.
        self.forcetoenablemotion = 0 
        # Sound to make when punted : TODO: Replace this filler.
        self.puntsound = ""


        self.auto_properties.extend(["targetname", "globalname", "angles", "model", "skin", "modelscale", "disableshadows", "targetname", "damagefilter", "disableshadows", "ExplodeDamage", "ExplodeRadius", "PerformanceMode", "BreakModelMessage", "spawnflags", "pressuredelay", "mindxlevel", "maxdxlevel", "fademindist", "fademaxdist", "fadescale", "spawnflags", "minhealthdmg", "shadowcastdist", "physdamagescale", "Damagetype", "nodamageforces", "inertiaScale", "massScale", "overridescript", "damagetoenablemotion", "forcetoenablemotion", "puntsound"])


class PropCoreball(Entity):
    """
    Auto-generated from halflife2.fgd, line 4590.
    Core Ball
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_coreball", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 


        self.auto_properties.extend(["targetname", "angles"])


class PropScalable(Entity):
    """
    Auto-generated from halflife2.fgd, line 4597.
    Scalable Prop
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_scalable", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Model Scale : A multiplier for the size of the model.
        self.modelscale = "1.0" 
        # Disable Shadows : Used to disable dynamic shadows on this entity.
        self.disableshadows = 0 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Render FX : TODO: Replace this filler.
        self.renderfx = 0 

        # Render Mode : Used to set a non-standard rendering mode on this entity. See also 'FX Amount' and 'FX Color'.
        self.rendermode = 0 
        # FX Amount (0 - 255) : The FX amount is used by the selected Render Mode.
        self.renderamt = 255 
        # FX Color (R G B) : The FX color is used by the selected Render Mode.
        self.rendercolor = "255 255 255" 
        # Disable Receiving Shadows : TODO: Replace this filler.
        self.disablereceiveshadows = 0 


        self.auto_properties.extend(["targetname", "model", "skin", "modelscale", "disableshadows", "angles", "renderfx", "rendermode", "renderamt", "rendercolor", "disablereceiveshadows"])


class PropThumper(Entity):
    """
    Auto-generated from halflife2.fgd, line 481.
    Thumper
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_thumper", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # World model : TODO: Replace this filler.
        self.model = "models/props_combine/CombineThumper002.mdl"
        # Dust Scale : TODO: Replace this filler.
        self.dustscale = "Small Thumper" 
        # Effect Radius : Radius of the thumper's sound. EPISODIC ONLY.
        self.EffectRadius = 1000 

        self.auto_properties.extend(["targetname", "angles", "model", "dustscale", "EffectRadius"])


class PropVehicle(Entity):
    """
    Auto-generated from halflife2.fgd, line 4268.
    Studiomodel vehicle that can be driven via inputs.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "spawnflags"])


class PropVehicleAirboat(Entity):
    """
    Auto-generated from halflife2.fgd, line 4386.
    Driveable studiomodel airboat.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle_airboat", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 

        # World model : TODO: Replace this filler.
        self.model = "models/airboat.mdl"
        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/airboat.txt"
        # Has Gun : Whether the airboat's gun is enabled or disabled.
        self.EnableGun = 0 

        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked", "model", "vehiclescript", "EnableGun"])


class PropVehicleApc(Entity):
    """
    Auto-generated from halflife2.fgd, line 4313.
    APC with mounted guns, can only be driven by a vehicle driver or a npc_apcdriver.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle_apc", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 

        # Missile Hint Target : Name of one or more info_apc_missile_hint entities to use to determine what to hit.
        self.missilehint = "" 

        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked", "missilehint"])


class PropVehicleCannon(Entity):
    """
    Auto-generated from halflife2.fgd, line 4401.
    Driveable studiomodel cannon.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle_cannon", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 


        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked"])


class PropVehicleChoreoGeneric(Entity):
    """
    Auto-generated from halflife2.fgd, line 4532.
    Generic Choreo vehicle used for magical events.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle_choreo_generic", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # World model : TODO: Replace this filler.
        self.model = "models/vehicles/prisoner_pod.mdl"
        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/choreo_vehicle.txt"
        # Ignore Move Parent on Exit : Should the player ignore this vehicle's move parent went performing exit checks.
        self.ignoremoveparent = 0 
        # Ignore Player collision : The player wont collide against this vehicle when moving around.
        self.ignoreplayer = 0 

        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked", "parentname", "model", "vehiclescript", "ignoremoveparent", "ignoreplayer"])


class PropVehicleCrane(Entity):
    """
    Auto-generated from halflife2.fgd, line 4407.
    Driveable studiomodel crane.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle_crane", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 

        # Magnet entity : TODO: Replace this filler.
        self.magnetname = ""

        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked", "magnetname"])


class PropVehicleDriveable(Entity):
    """
    Auto-generated from halflife2.fgd, line 4282.
    Generic driveable studiomodel vehicle.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle_driveable", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 


        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked"])


class PropVehicleJeep(Entity):
    """
    Auto-generated from halflife2.fgd, line 4336.
    Driveable studiomodel jeep.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle_jeep", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 

        # Hopper Visible : Is the striderbuster cargo hopper visible?
        self.CargoVisible = 0 
        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""

        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked", "CargoVisible", "spawnflags"])


class PropVehiclePrisonerPod(Entity):
    """
    Auto-generated from halflife2.fgd, line 4415.
    Combine prisoner pod that the player can ride in.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "prop_vehicle_prisoner_pod", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # World model : TODO: Replace this filler.
        self.model = "models/vehicles/prisoner_pod.mdl"
        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/prisoner_pod.txt"

        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked", "parentname", "model", "vehiclescript"])


class ScriptIntro(Entity):
    """
    Auto-generated from halflife2.fgd, line 4449.
    Script: Custom entity used to handle the intro sequence.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "script_intro", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Match env_zoom's FOV transition : Whether the script should match env_zoom's FOV transition.
        self.alternatefovchange = 0 

        self.auto_properties.extend(["targetname", "alternatefovchange"])


class ScriptTauremoval(Entity):
    """
    Auto-generated from halflife2.fgd, line 4440.
    Script: Custom entity used to handle the tau removal sequence in coast. (unused)
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "script_tauremoval", vmf_map)

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Vortigaunt to use : TODO: Replace this filler.
        self.vortigaunt = ""

        self.auto_properties.extend(["angles", "targetname", "parentname", "vortigaunt"])


class ScriptedSentence(Entity):
    """
    Auto-generated from halflife2.fgd, line 2148.
    Scripted Sentence
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "scripted_sentence", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Sentence Name : TODO: Replace this filler.
        self.sentence = ""
        # Speaker Type : TODO: Replace this filler.
        self.entity = ""
        # Additional Sentence Time : TODO: Replace this filler.
        self.delay = "0"
        # Search Radius : TODO: Replace this filler.
        self.radius = 512
        # Delay Before Refire : TODO: Replace this filler.
        self.refire = "3"
        # Listener Type : TODO: Replace this filler.
        self.listener = ""
        # Volume 0-10 : TODO: Replace this filler.
        self.volume = "10"
        # Sound Radius : TODO: Replace this filler.
        self.attenuation = 0 

        self.auto_properties.extend(["targetname", "spawnflags", "sentence", "entity", "delay", "radius", "refire", "listener", "volume", "attenuation"])


class ScriptedSequence(Entity):
    """
    Auto-generated from halflife2.fgd, line 2952.
    Grabs an NPC and makes them play a specified set of animations. The NPC can be told to move to the scripted sequence position or can be told to play the script wherever they currently are. Multiple scripted sequences of the same name will frame-synchronize in the action animation once all the actors have moved to position. This allows tight interaction between actors (one actor grabbing another, hitting them, etc.) The flow is as follows:\n\n1) Move to position using the specified movement animation. If 'Move to Position' is set to NONE, skip to step 2.\n2) If forced to wait for another actor to move to position, play the pre-action idle animation, otherwise skip to step 3. If there is no pre-action idle specified, ACT_IDLE is used.\n3) Fire the OnBeginSequence output.\n4) Play the action animation. If no action animation is specified, skip to step 5.\n5) Play the post-action idle animation. If none is specified, skip to step 6. If the 'Loop in Post Idle' spawnflag is set, keep playing the post-action idle until the script is cancelled. If no post-action idle animation is specified, ACT_IDLE is used.\n6) Fire the OnEndSequence output.\n7) If a next script to play is specified, hand the NPC to the next script and repeat this process for that script.\n\nThe MoveToPosition input runs steps 1 and 2, then waits while playing the pre-action idle animation until the BeginSequence input is received.\n\nIf the sequence has motion extraction in it, set the 'Don't Teleport NPC On End' spawnflag.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "scripted_sequence", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Target NPC : The name or class name (such as 'npc_zombie') of an NPC to use for this script.
        self.m_iszEntity = ""
        # Pre Action Idle Animation : The name of the sequence (such as 'idle01') or activity (such as 'ACT_IDLE') to play before the action animation if the NPC must wait for the script to be triggered. Use 'Start on Spawn' flag or MoveToPosition input to play this idle animation.
        self.m_iszIdle = "" 
        # Entry Animation : The name of the sequence (such as 'reload02') or activity (such as 'ACT_RELOAD') to play when the sequence starts, before transitioning to play the main action sequence.
        self.m_iszEntry = "" 
        # Action Animation : The name of the main sequence (such as 'reload02') or activity (such as 'ACT_RELOAD') to play.
        self.m_iszPlay = "" 
        # Post Action Idle Animation : The name of the sequence (such as 'idle01') or activity (such as 'ACT_IDLE') to play after the action animation.
        self.m_iszPostIdle = "" 
        # Custom Move Animation : Used in conjunction with the 'Custom movement' setting for the 'Move to Position' property, specifies the sequence (such as 'crouch_run01') or activity (such as 'ACT_RUN') to use while moving to the scripted position.
        self.m_iszCustomMove = "" 
        # Loop Action Animation? : TODO: Replace this filler.
        self.m_bLoopActionSequence = 0 
        # Synch Post Idles? : TODO: Replace this filler.
        self.m_bSynchPostIdles = 0 
        # Search Radius (0=everywhere) : Radius to search within for an NPC to use. 0 searches everywhere.
        self.m_flRadius = 0 
        # Repeat Rate ms : TODO: Replace this filler.
        self.m_flRepeat = 0
        # Move to Position : TODO: Replace this filler.
        self.m_fMoveTo = 1 
        # Next Script : The name of the script to run immediately after this script completes. The NPC will not return to AI between the two scripts.
        self.m_iszNextScript = ""
        # Ignore Gravity on NPC during script : If this is set to 'Yes', the NPC will not be subject to gravity while playing this script.
        self.m_bIgnoreGravity = 0 
        # Disable NPC collisions during script : Useful for when NPCs playing scripts must interpenetrate while riding on trains, elevators, etc. This only disables collisions between the NPCs in the script and must be enabled on BOTH scripted_sequences.
        self.m_bDisableNPCCollisions = 0 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # On player death : What should this entity do if the player dies
        self.onplayerdeath = 0 

        self.auto_properties.extend(["targetname", "parentname", "angles", "m_iszEntity", "m_iszIdle", "m_iszEntry", "m_iszPlay", "m_iszPostIdle", "m_iszCustomMove", "m_bLoopActionSequence", "m_bSynchPostIdles", "m_flRadius", "m_flRepeat", "m_fMoveTo", "m_iszNextScript", "m_bIgnoreGravity", "m_bDisableNPCCollisions", "angles", "mindxlevel", "maxdxlevel", "spawnflags", "onplayerdeath"])


class ScriptedTarget(Entity):
    """
    Auto-generated from halflife2.fgd, line 2182.
    Scripted Target
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "scripted_target", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 1 
        # Target NPC : TODO: Replace this filler.
        self.m_iszEntity = ""
        # Search Radius (0=everywhere) : Radius to search within for an NPC to use. 0 searches everywhere.
        self.m_flRadius = 0 
        # Move Speed : TODO: Replace this filler.
        self.MoveSpeed = 5			
        # Pause Duration : TODO: Replace this filler.
        self.PauseDuration = 0		
        # Effect Duration : TODO: Replace this filler.
        self.EffectDuration = 2		
        # Next Target : TODO: Replace this filler.
        self.target = ""

        self.auto_properties.extend(["targetname", "parentname", "StartDisabled", "m_iszEntity", "m_flRadius", "MoveSpeed", "PauseDuration", "EffectDuration", "target"])


class TestSidelist(Entity):
    """
    Auto-generated from halflife2.fgd, line 4511.
    Test entity for Ken!
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "test_sidelist", vmf_map)

        # Sides : TODO: Replace this filler.
        self.sides = ""

        self.auto_properties.extend(["sides"])


class TriggerPhysicsTrap(Entity):
    """
    Auto-generated from halflife2.fgd, line 137.
    A volumetric trigger that disintegrates enemies
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "trigger_physics_trap", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Filter Name : Filter to use to see if activator triggers me. See filter_activator_name for more explanation.
        self.filtername = ""


        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Dissolve Type : TODO: Replace this filler.
        self.dissolvetype = "Energy" 

        self.auto_properties.extend(["targetname", "parentname", "origin", "StartDisabled", "targetname", "parentname", "origin", "StartDisabled", "globalname", "spawnflags", "filtername", "angles", "dissolvetype"])


class TriggerRpgfire(Entity):
    """
    Auto-generated from halflife2.fgd, line 3577.
    A volumetric trigger that triggers whenever an RPG is fired within it.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "trigger_rpgfire", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Filter Name : Filter to use to see if activator triggers me. See filter_activator_name for more explanation.
        self.filtername = ""



        self.auto_properties.extend(["targetname", "parentname", "origin", "StartDisabled", "targetname", "parentname", "origin", "StartDisabled", "globalname", "spawnflags", "filtername"])


class TriggerVphysicsMotion(Entity):
    """
    Auto-generated from halflife2.fgd, line 3584.
    A volumetric trigger that affects the motion of vphysics objects that touch it.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "trigger_vphysics_motion", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Filter Name : Filter to use to see if activator triggers me. See filter_activator_name for more explanation.
        self.filtername = ""


        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 
        # Filter Name : Filter to use to see if activator triggers me. See filter_activator_name for more explanation.
        self.filtername = ""
        # Scale gravity of objects in the field. : TODO: Replace this filler.
        self.SetGravityScale = "1.0"
        # Additional air density for drag : TODO: Replace this filler.
        self.SetAdditionalAirDensity = "0"
        # Max velocity in field (0 disables) : TODO: Replace this filler.
        self.SetVelocityLimit = "0.0"
        # Max amount to reduce velocity per second when it exceeds the velocity limit (0 disables) : TODO: Replace this filler.
        self.SetVelocityLimitDelta = "0.0"
        # Velocity scale/drag : TODO: Replace this filler.
        self.SetVelocityScale = "1.0"
        # Max angular velocity in field (degrees/s, 0 disables) : TODO: Replace this filler.
        self.SetAngVelocityLimit = "0.0"
        # Angular Velocity scale/drag : TODO: Replace this filler.
        self.SetAngVelocityScale = "1.0"
        # Linear force (0 disables) : TODO: Replace this filler.
        self.SetLinearForce = "0.0"
        # Direction of linear force (Pitch Yaw Roll (Y Z X)) : TODO: Replace this filler.
        self.SetLinearForceAngles = "0 0 0"
        # Particle Trail Material : Name of a material to use for the particle trail, no name means no particle trail
        self.ParticleTrailMaterial = ""
        # Particle Trail Lifetime : Lifetime of the particles to emit
        self.ParticleTrailLifetime = 4 
        # Particle Trail Starting Sprite Size : Starting size of the sprite to emit
        self.ParticleTrailStartSize = 2 
        # Particle Trail Ending Sprite Size : Ending size of the sprite to emit
        self.ParticleTrailEndSize = 3 

        self.auto_properties.extend(["targetname", "parentname", "origin", "StartDisabled", "targetname", "parentname", "origin", "StartDisabled", "globalname", "spawnflags", "filtername", "spawnflags", "StartDisabled", "filtername", "SetGravityScale", "SetAdditionalAirDensity", "SetVelocityLimit", "SetVelocityLimitDelta", "SetVelocityScale", "SetAngVelocityLimit", "SetAngVelocityScale", "SetLinearForce", "SetLinearForceAngles", "ParticleTrailMaterial", "ParticleTrailLifetime", "ParticleTrailStartSize", "ParticleTrailEndSize"])


class TriggerWaterydeath(Entity):
    """
    Auto-generated from halflife2.fgd, line 3114.
    A trigger volume that spawns leeches around entities inside it, and does damage to them until they die. Used to prevent players entering deep water.
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "trigger_waterydeath", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Filter Name : Filter to use to see if activator triggers me. See filter_activator_name for more explanation.
        self.filtername = ""



        self.auto_properties.extend(["targetname", "parentname", "origin", "StartDisabled", "targetname", "parentname", "origin", "StartDisabled", "globalname", "spawnflags", "filtername"])


class TriggerWeaponDissolve(Entity):
    """
    Auto-generated from halflife2.fgd, line 148.
    A volumetric trigger that dissolves all weapons within it
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "trigger_weapon_dissolve", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Filter Name : Filter to use to see if activator triggers me. See filter_activator_name for more explanation.
        self.filtername = ""


        # Emitter Name : Name of a single or multiple entities to use as the basis for the emitted effects.
        self.emittername = "" 

        self.auto_properties.extend(["targetname", "parentname", "origin", "StartDisabled", "targetname", "parentname", "origin", "StartDisabled", "globalname", "spawnflags", "filtername", "emittername"])


class TriggerWeaponStrip(Entity):
    """
    Auto-generated from halflife2.fgd, line 161.
    A volumetric trigger that strips combat characters of all weapons
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "trigger_weapon_strip", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Parent : The name of this entity's parent in the movement hierarchy. Entities with parents move with their parent.
        self.parentname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Start Disabled : TODO: Replace this filler.
        self.StartDisabled = 0 

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Filter Name : Filter to use to see if activator triggers me. See filter_activator_name for more explanation.
        self.filtername = ""


        # Kill Weapons : TODO: Replace this filler.
        self.KillWeapons = "No" 

        self.auto_properties.extend(["targetname", "parentname", "origin", "StartDisabled", "targetname", "parentname", "origin", "StartDisabled", "globalname", "spawnflags", "filtername", "KillWeapons"])


class VehicleViewcontroller(Entity):
    """
    Auto-generated from halflife2.fgd, line 4379.
    Vehicle hack to control player view
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "vehicle_viewcontroller", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Origin (X Y Z) : The position of this entity's center in the world. Rotating entities typically rotate around their origin.
        self.origin = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Collisions : TODO: Replace this filler.
        self.solid = 6 
        # Disable Shadows : TODO: Replace this filler.
        self.disableshadows = 0 
        # Screen Space Fade : The method by which the fading distance should be determined. If 'No', the fade distances is the distance from the player's view to the object, in inches. If 'Yes', the fade distance is the size of the object onscreen, in pixels.
        self.screenspacefade = 0 
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7 [hl2/ep1/portal] or dx8 [ep2/tf], then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 
        # Lighting Origin : Select an info_lighting to specify a location to sample lighting from, instead of using this entity's origin.
        self.lightingorigin = "" 
        # Disable Vertex lighting : TODO: Replace this filler.
        self.disablevertexlighting = 0 
        # Disable Self-Shadowing with vertex lighting : TODO: Replace this filler.
        self.disableselfshadowing = 0 
        # Ignore surface normal for computing vertex lighting : TODO: Replace this filler.
        self.ignorenormals = 0 
        # Generate (and use) lightmaps for this static prop : TODO: Replace this filler.
        self.generatelightmaps = 0 
        # Lightmap Resolution X : The resolution of the generated lightmap in the X (or U) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutionx = 32 
        # Lightmap Resolution Y : The resolution of the generated lightmap in the Y (or V) direction (only used if Generate Lightmaps is Yes) 
        self.lightmapresolutiony = 32 

        # Vehicle Script File : TODO: Replace this filler.
        self.vehiclescript = "scripts/vehicles/jeep_test.txt"
        # Scale of action input / framerate : TODO: Replace this filler.
        self.actionScale = "1"

        # Start locked : TODO: Replace this filler.
        self.VehicleLocked = 0 


        self.auto_properties.extend(["targetname", "origin", "globalname", "angles", "mindxlevel", "maxdxlevel", "model", "skin", "solid", "disableshadows", "screenspacefade", "fademindist", "fademaxdist", "fadescale", "lightingorigin", "disablevertexlighting", "disableselfshadowing", "ignorenormals", "generatelightmaps", "lightmapresolutionx", "lightmapresolutiony", "vehiclescript", "actionScale", "VehicleLocked"])


class Weapon357(Entity):
    """
    Auto-generated from halflife2.fgd, line 3567.
    357
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_357", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponAlyxgun(Entity):
    """
    Auto-generated from halflife2.fgd, line 3573.
    Alyx Gun
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_alyxgun", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponAnnabelle(Entity):
    """
    Auto-generated from halflife2.fgd, line 3574.
    Annabelle (Grigori)
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_annabelle", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponAr2(Entity):
    """
    Auto-generated from halflife2.fgd, line 3564.
    Assault Rifle 2
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_ar2", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponBrickbat(Entity):
    """
    Auto-generated from halflife2.fgd, line 3665.
    Brickbat
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_brickbat", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # BrickbatType : TODO: Replace this filler.
        self.BrickbatType = "Rock" 

        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale", "BrickbatType"])


class WeaponBugbait(Entity):
    """
    Auto-generated from halflife2.fgd, line 3572.
    Bug bait
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_bugbait", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponCrossbow(Entity):
    """
    Auto-generated from halflife2.fgd, line 3568.
    Crossbow
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_crossbow", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponCrowbar(Entity):
    """
    Auto-generated from halflife2.fgd, line 3561.
    Crowbar
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_crowbar", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponFrag(Entity):
    """
    Auto-generated from halflife2.fgd, line 3570.
    Frag Grenade
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_frag", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponPhyscannon(Entity):
    """
    Auto-generated from halflife2.fgd, line 3571.
    Physics Cannon
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_physcannon", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponPistol(Entity):
    """
    Auto-generated from halflife2.fgd, line 3563.
    Pistol
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_pistol", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponRpg(Entity):
    """
    Auto-generated from halflife2.fgd, line 3565.
    Missile Launcher
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_rpg", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponShotgun(Entity):
    """
    Auto-generated from halflife2.fgd, line 3569.
    Shotgun
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_shotgun", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponSmg1(Entity):
    """
    Auto-generated from halflife2.fgd, line 3566.
    SMG1
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_smg1", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


class WeaponStriderbuster(Entity):
    """
    Auto-generated from halflife2.fgd, line 4645.
    Strider Buster
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_striderbuster", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Global Entity Name : Name by which this entity is linked to another entity in a different map. When the player transitions to a new map, entities in the new map with globalnames matching entities in the previous map will have the previous map's state copied over their state.
        self.globalname = "" 

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # World Model : TODO: Replace this filler.
        self.model = ""
        # Skin : Some models have multiple versions of their textures, called skins. Set this to a number other than 0 to use that skin instead of the default.
        self.skin = 0 
        # Model Scale : A multiplier for the size of the model.
        self.modelscale = "1.0" 
        # Disable Shadows : Used to disable dynamic shadows on this entity.
        self.disableshadows = 0 

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Damage Filter : Name of the filter entity that controls which entities can damage us.
        self.damagefilter = "" 

        # Disable shadows : TODO: Replace this filler.
        self.disableshadows = 0 

        # Explosion Damage : If non-zero, when this entity breaks it will create an explosion that causes the specified amount of damage. See also 'Explosion Radius'.
        self.ExplodeDamage = 0 
        # Explosion Radius : If non-zero, when this entity breaks it will create an explosion with a radius of the specified amount. See also 'Explosion Damage'.
        self.ExplodeRadius = 0 
        # Performance Mode : Used to limit the amount of gibs produced when this entity breaks, for performance reasons.
        self.PerformanceMode = 0 
        # Break Model Message : If set, will use this break model message instead of the normal break behavior.
        self.BreakModelMessage = "" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Pressure Delay : Delay, in seconds, after 'broken' by pressure before breaking apart (allows for sound to play before breaking apart).
        self.pressuredelay = 0 

        # Minimum DX Level : TODO: Replace this filler.
        self.mindxlevel = 0 
        # Maximum DX Level : TODO: Replace this filler.
        self.maxdxlevel = 0 

        # Start Fade Dist : Distance at which the prop starts to fade (<0 = use fademaxdist).
        self.fademindist = -1 
        # End Fade Dist : Max fade distance at which the prop is visible (0 = don't fade out)
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Min Damage to Hurt : The prop will ignore any damage events if the damage is less than this amount.
        self.minhealthdmg = 0 
        # Shadow Cast Distance : Use this to override how far this object casts shadows. 0 = default distance.
        self.shadowcastdist = 0 
        # Physics Impact Damage Scale : Scales damage energy when this object is hit by a physics object. NOTE: 0 means this feature is disabled for backwards compatibility.\nSet to 1.0 for materials as strong as flesh, smaller numbers indicate stronger materials.
        self.physdamagescale = "0.1" 
        # Impact damage type : TODO: Replace this filler.
        self.Damagetype = 0 
        # Damaging it Doesn't Push It : Used to determine whether or not damage should cause the brush to move.
        self.nodamageforces = 0 
        # Scale Factor For Inertia : Scales the angular mass of an object. Used to hack angular damage and collision response.
        self.inertiaScale = "1.0" 
        # Mass Scale : A scale multiplier for the object's mass.
        self.massScale = "0" 
        # Override Parameters : A list of physics key/value pairs that are usually in a physics prop .qc file. Format is 'key,value,key,value,etc'.
        self.overridescript = "" 
        # Health Level to Override Motion : If specified, this object will start motion disabled. Once its health has dropped below this specified amount, it will enable motion.
        self.damagetoenablemotion = 0 
        # Physics Impact Force to Override Motion : If specified, this object will start motion disabled. Any impact that imparts a force greater than this value on the physbox will enable motion.
        self.forcetoenablemotion = 0 
        # Sound to make when punted : TODO: Replace this filler.
        self.puntsound = ""

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Dud Bomb : This striderbuster is not armed (to be used for training).
        self.dud = 0 

        self.auto_properties.extend(["targetname", "globalname", "angles", "model", "skin", "modelscale", "disableshadows", "targetname", "damagefilter", "disableshadows", "ExplodeDamage", "ExplodeRadius", "PerformanceMode", "BreakModelMessage", "spawnflags", "pressuredelay", "mindxlevel", "maxdxlevel", "fademindist", "fademaxdist", "fadescale", "spawnflags", "minhealthdmg", "shadowcastdist", "physdamagescale", "Damagetype", "nodamageforces", "inertiaScale", "massScale", "overridescript", "damagetoenablemotion", "forcetoenablemotion", "puntsound", "spawnflags", "dud"])


class WeaponStunstick(Entity):
    """
    Auto-generated from halflife2.fgd, line 3562.
    StunStick
    """
    def __init__(self, vmf_map):
        Entity.__init__(self, "weapon_stunstick", vmf_map)

        # Name : The name that other entities refer to this entity by.
        self.targetname = ""

        # Pitch Yaw Roll (Y Z X) : This entity's orientation in the world. Pitch is rotation around the Y axis, 
        self.angles = "0 0 0" 

        # TODO: Replace this filler. : TODO: Replace this filler.
        self.spawnflags = ""
        # Start Fade Dist/Pixels : Distance at which the prop starts to fade (<0 = use fademaxdist). If 'Screen Space Fade' is selected, this represents the number of pixels wide covered by the prop when it starts to fade.
        self.fademindist = -1 
        # End Fade Dist/Pixels : Maximum distance at which the prop is visible (0 = don't fade out). If 'Screen Space Fade' is selected, this represents the *minimum* number of pixels wide covered by the prop when it fades.
        self.fademaxdist = 0 
        # Fade Scale : If you specify a fade in the worldspawn, or if the engine is running under dx7, then the engine will forcibly fade out props even if fademindist/fademaxdist isn't specified.
        self.fadescale = 1 


        self.auto_properties.extend(["targetname", "angles", "spawnflags", "fademindist", "fademaxdist", "fadescale"])


